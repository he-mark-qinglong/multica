# `_shared.strategy_kit.templates.meta_label_template.strategy`

Source: `_shared/strategy_kit/templates/meta_label_template/strategy.py`

Meta-labeling strategy template (A11).

## `generate_signals(bars: 'Dict[str, pd.DataFrame]', config: 'Dict') -> 'List[Trade]'`

Momentum primary + meta-label filter -> closed trades.

| Parameter | Type | Default |
|---|---|---|
| `bars` | 'Dict[str, pd.DataFrame]' | — |
| `config` | 'Dict' | — |
