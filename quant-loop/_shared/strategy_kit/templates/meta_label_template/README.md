# meta_label_template

de Prado meta-labeling wiring demo — ROC momentum as the primary model, same-side triple-barrier label as the meta filter (in-sample by construction; replace the filter with a fitted meta-model for production).
