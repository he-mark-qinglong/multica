# 片 w1-s4 — Round-2 任务卡：PF 口径统一 + vectorbt 纳入 G5、new_variant scaffold、CI 脚本 bash 3.2 兼容

> 执行者：caocao-m3 cheap agent，零上下文，单卡 <30 min。
> 所有 pytest 命令的工作目录（cwd）均为 `/Users/mark/multica/quant-loop`。
> Python 一律 `$PY` = `/Users/mark/sdk/mamba-envs/trading/bin/python3`。
> **仓库 worktree 里有别人的未提交改动：只改卡片列出的文件，绝不 `git add/commit/checkout/restore`。**
> 基线（2026-07-25 实测）：`$PY -m pytest validation/ _shared/ -q` → 142 passed, 3 skipped。你的改动不得让这个数字变红。

---

## T9a — compute_metrics 的 profit_factor 口径统一（trade-based）

- **目标**：`_shared/validation/compute_metrics.py` 在传入 `trade_pnls` 时，`profit_factor` 改按 trade pnl 计算（与 `validation/metrics.py:84-91` 完全一致）；不传时保留 bar-based 口径并在 docstring 标注。消除"同一个 key 两个含义"。

- **读**（已核实存在）：
  - `_shared/validation/compute_metrics.py`（118 行；PF 现状在 `:84-87` 按 bar return 算；win_rate 的 trade/bar 双口径范式在 `:89-96`，照抄这个模式）
  - `validation/metrics.py:84-91`（目标语义：`losses<=0 → inf if gains>0 else 0.0`，否则 `gains/losses`）
  - `_shared/validation/test_compute_metrics.py`（154 行）

- **写**（仅这两个文件）：
  - `_shared/validation/compute_metrics.py`
  - `_shared/validation/test_compute_metrics.py`

- **步骤**：
  1. 改 `compute_metrics.py`：把 `:84-87` 的 PF 计算移入 `trade_pnls is not None` 分支。结构改成（伪码，照 `:89-96` win_rate 的写法）：
     ```python
     if trade_pnls is not None:
         pnls = np.asarray(list(trade_pnls), dtype=float)
         pnls = pnls[np.isfinite(pnls)]
         gains = pnls[pnls > 0].sum()
         losses = -pnls[pnls < 0].sum()
         if losses <= 0:
             pf = float("inf") if gains > 0 else 0.0
         else:
             pf = float(gains / losses)
         win_rate = float((pnls > 0).mean()) if pnls.size else 0.0
     else:
         pos = rets[rets > 0].sum(); neg = -rets[rets < 0].sum()
         pf = _safe_div(pos, neg, default=0.0)          # legacy bar-based
         win_rate = int((rets > 0).sum()) / max(n_bars, 1)
     ```
     注意 `trade_pnls` 的 array 预处理只写一次，PF 和 win_rate 共用（现在 win_rate 分支里已有一份，合并）。
  2. 更新 docstring（`:28-44`）：`trade_pnls` 参数说明补一句 "profit_factor is also computed per trade (gross profit / abs(gross loss), inf when no losers), matching validation/metrics.py:84-91"；Returns 段注明不传 `trade_pnls` 时 PF 是 legacy bar-return 口径。
  3. 改测试文件：
     - 现有用例**不得删改语义**：`test_compute_metrics_pf_uses_bar_returns_not_dollars`（:96-106）和 `test_compute_metrics_uptrend_has_positive_sharpe_and_ann`（:66-83，断言 PF==0.0）都不传 `trade_pnls`，走 bar fallback，应保持绿。
     - 新增 3 个用例：
       a. 传入 `trade_pnls=[0.01, -0.02, 0.03, -0.01]` → `profit_factor == pytest.approx(0.04/0.03)`（equity 用全涨曲线，使 bar-based PF 会是 0，以此证明走的是 trade 路径——可仿照 :122-131 win_rate 用例的构造）；
       b. `trade_pnls=[0.01, 0.02]`（无亏损）→ `profit_factor == float("inf")`；
       c. `trade_pnls=[]` → `profit_factor == 0.0`。
  4. **不要**改 `validation/metrics.py`——它的 `metrics_from_run`（:102-124）PF 本来就自己调 `profit_factor(pnls)`（:117），本卡改完后语义恰好一致，无需动。

- **验收**：
  ```bash
  cd /Users/mark/multica/quant-loop && /Users/mark/sdk/mamba-envs/trading/bin/python3 -m pytest _shared/validation/test_compute_metrics.py validation/test_metrics.py -q
  ```
  预期：全绿（旧用例不红 + 新 3 用例通过）。
  不变式：`grep -n "bar" _shared/validation/compute_metrics.py` 中 docstring 必须出现 bar-based 口径说明。

- **est**: 20 min | **machine**: mac（$PY 是 mac 本地 mamba env）| **deps**: 无

---

## T9b — evaluate_gates 增加 window_vectorbt 参数并纳入 G5

- **目标**：`validation/gates.py` 的 `evaluate_gates` 新增可选参数 `window_vectorbt`，非空时并入 G5 的 `framework_means`（G5 取各框架 mean OOS Sharpe 的最差值）。签名向后兼容（默认 None），本卡**不改任何调用方**（调用方接线是 T9c）。

- **读**：
  - `validation/gates.py`（187 行；G5 现状 `:130-138` 只消费 backtrader+freqtrade；模块 docstring gate 表 `:8-17` 第 14 行写 "framework CV (backtrader/freqtrade)"）
  - `validation/test_gates.py`（151 行；`_passing_kwargs()` 在 `:30-38`，`_windows()` helper 在 `:26-27`）

- **写**（仅这两个文件）：
  - `validation/gates.py`
  - `validation/test_gates.py`

- **步骤**：
  1. `gates.py` 签名（`:92-102`）：在 `window_freqtrade: list[dict],` 之后加一行 `window_vectorbt: list[dict] | None = None,`（保持 keyword-only，现有调用方全是 kwargs 调用，不会破坏）。
  2. `gates.py` G5 块（`:133-138`）：在 freqtrade 的 if 块后追加：
     ```python
     if window_vectorbt:
         framework_means.append(_mean([m["sharpe"] for m in window_vectorbt]))
     ```
  3. `gates.py` 模块 docstring `:14`：`framework CV (backtrader/freqtrade)` 改为 `framework CV (backtrader/freqtrade/vectorbt — worst of whichever legs ran)`。
  4. `gates.py` `:107-109` 的 docstring `window_*` 说明补一句 vectorbt。
  5. `test_gates.py` 新增 2 个用例（照 `test_g5_fails_when_worst_framework_below_one` :98-104 的模式）：
     - `test_g5_fails_when_vectorbt_worst`：`kw["window_vectorbt"] = _windows(sharpe=0.4)` → G5 不过、`observed == pytest.approx(0.4)`；
     - `test_g5_vectorbt_omitted_keeps_legacy_behavior`：不传 `window_vectorbt`，bt=1.2/ft=1.1 → G5 过、`observed == pytest.approx(1.1)`。
  6. **不要**改 `validation/generic_harness.py` / `validation/oos_harness.py`（T9c 的活，本卡动了会和 T8 撞车）。

- **验收**：
  ```bash
  cd /Users/mark/multica/quant-loop && /Users/mark/sdk/mamba-envs/trading/bin/python3 -m pytest validation/test_gates.py -q
  ```
  预期：全绿（原 12 个用例语义不变 + 新 2 个通过）。
  不变式：`grep -n "window_vectorbt" validation/gates.py | wc -l` >= 3（签名 + G5 块 + docstring）。

- **est**: 15 min | **machine**: mac | **deps**: 无（与 T9a 文件不相交，可并行）

---

## T9c — 两个 harness 把 vectorbt 窗口接进 evaluate_gates

- **目标**：`validation/generic_harness.py` 和 `validation/oos_harness.py` 的 `evaluate_gates` 调用点把已经算好的 vectorbt 窗口传进去（现在两条管线都算了 vectorbt leg 但丢在地上不用）。

- **读**：
  - `validation/generic_harness.py:346-354`（调用点；`window_by_framework` dict 在 `:292-293` 构造，vectorbt leg 结果在 `:326` append）
  - `validation/oos_harness.py:94`（`window_vbt` 列表已存在）、`:134-142`（vectorbt leg 填充）、`:167-175`（调用点，没传 `window_vbt`）
  - `validation/test_generic_harness.py`（316 行）

- **写**（仅这三个文件）：
  - `validation/generic_harness.py`
  - `validation/oos_harness.py`
  - `validation/test_generic_harness.py`

- **步骤**：
  1. `generic_harness.py:346-354`：在 `window_freqtrade=window_by_framework.get("freqtrade", []),` 后加一行：
     ```python
     window_vectorbt=window_by_framework.get("vectorbt", []),
     ```
     （`.get(..., [])` 必须保留——`window_by_framework` 只在 frameworks 含 vectorbt 时才有该 key，见 `:292-293`。）
  2. `oos_harness.py:167-175`：在 `window_freqtrade=window_ft,` 后加一行 `window_vectorbt=window_vbt,`。
  3. `test_generic_harness.py` 新增 1 个用例：monkeypatch `validation.generic_harness.evaluate_gates` 为一个捕获 kwargs 的假函数（返回一个 `SimpleNamespace(passed=True, gates=[], summary_lines=lambda: [])`），用 `gh.run_generic_validation(dummy_signals, CONFIG, _synthetic_data(), n_windows=2, frameworks=["native"], output_dir=tmp_path)` 跑一遍，断言捕获到的 kwargs 里 `window_vectorbt == []`（frameworks 不含 vectorbt 时也要传空列表，key 必须存在）。helper `_synthetic_data`/`dummy_signals`/`CONFIG` 直接用文件里现有的（`:30-63`）。
  4. 若 T9b 未先落地（`evaluate_gates` 还不认 `window_vectorbt`），本卡验收会 TypeError——确认依赖完成再做。

- **验收**：
  ```bash
  cd /Users/mark/multica/quant-loop && /Users/mark/sdk/mamba-envs/trading/bin/python3 -m pytest validation/test_generic_harness.py validation/test_gates.py -q
  grep -n "window_vectorbt" validation/generic_harness.py validation/oos_harness.py
  ```
  预期：pytest 全绿；grep 两个文件各至少 1 行命中。

- **est**: 15 min | **machine**: mac | **deps**: **T9b 必须先完成**；**与 T8 有文件冲突**（T8 也改 `generic_harness.py` 和 `oos_harness.py`，见文末冲突节）——建议 T8 落地后再跑本卡。

---

## T10 — scripts/new_variant.py 新策略 scaffold 工具

- **目标**：`$PY scripts/new_variant.py <name> --timeframe 1h --symbols BTCUSDT,SOLUSDT` 一键生成可跑通 generic pipeline 的 `strategies/<name>_<YYYYMMDD>/` 骨架（contract-v2：config.json + data_loader.py + signals.py），并内置契约自检。这是"新策略 5 分钟接入"的核心交付物。

- **读**：
  - `validation/generic_harness.py:376-407`（`run_generic_from_variant` 的变体目录契约：必须有 `config.json`（含 `instruments` list + `timeframe`）、`data_loader.py` 暴露 `load_all(symbols, timeframe) -> {sym: df}`、`signals.py` 暴露 `generate_signals(df, cfg)`；**注意 harness 是按 symbol 单个 DataFrame 调 signals 的**，`SignalFn = Callable[[pd.DataFrame, dict], ...]` 在 `:58`）
  - `_shared/templates/strategy_contract_v2.py`（`check_contract` :185-218、`validate_module_signature` :91-126 **要求参数名必须叫 `bars` 和 `config`**、`validate_trades` :132-179 **要求返回 `_shared.run_backtest.Trade` 实例**、entry/exit 必须在 bar index 上、`make_synthetic_bars` :60-85）
  - `validation/test_generic_harness.py:250-286`（一个最小合法变体目录的 `_DATA_LOADER`/`_SIGNALS` 字符串模板——直接参考）
  - `quant-loop/data/` 数据布局（已核实）：`data/perp_<tf>/<SYM>_<tf>.parquet`，磁盘上只有 `perp_1m / perp_5m / perp_15m / perp_30m / perp_2h`（**没有 perp_1h**），1m 只有 BTC/ETH/SOL 三个 symbol
  - `_shared/run_backtest.py` 的 `Trade` dataclass（entry_ts/exit_ts/direction/size_fraction）
  - `scripts/test_backfill_aggtrades.py:18-21`（scripts 测试把脚本目录塞进 sys.path 的惯例）

- **写**（仅这两个新文件；**不改** `_shared/templates/` 任何现有文件）：
  - 新建 `scripts/new_variant.py`
  - 新建 `scripts/test_new_variant.py`

- **步骤**：
  1. `new_variant.py` 结构：`build_variant(name, timeframe, symbols, strategies_root) -> Path` 核心函数 + `main()` argparse 包装。CLI：位置参数 `name`，`--timeframe`（默认 `1h`），`--symbols`（逗号分隔，必填），可选 `--strategies-root`（默认 `Path(__file__).resolve().parents[1] / "strategies"`）。目录名 = `f"{name}_{datetime.now(timezone.utc):%Y%m%d}"`，已存在则报错退出（不覆盖）。
  2. 生成 `config.json`，键与 `run_generic_from_variant`/harness 默认值对齐：
     ```json
     {"timeframe": "<tf>", "instruments": ["BTCUSDT", ...],
      "fees_bps_per_side": 1.0, "slippage_bps_per_side": 1.0,
      "starting_capital_usd": 100000.0,
      "sizing": {"per_signal_weight_pct": 0.01, "max_gross_exposure_pct": 0.05}}
     ```
  3. 生成 `data_loader.py` 模板（字符串模板写进 new_variant.py）：`load_all(symbols, timeframe)` —— 先找 `<quant_loop_root>/data/perp_<tf>/<SYM>_<tf>.parquet`；**找不到且 tf != "1m" 时回退读 `perp_1m` 再 resample**（rule = tf，agg：open first / high max / low min / close last / volume sum；1m 没有就 raise FileNotFoundError 报清楚缺哪个文件）。`quant_loop_root = Path(__file__).resolve().parents[2]`。parquet 若有 `open_time` 列则设为 tz-UTC DatetimeIndex 并 sort_index。**禁止写死 `/home/smark`**。
  4. 生成 `signals.py` 模板——这是本卡最容易踩坑的地方，必须同时满足两个调用约定：
     - generic harness 调 `generate_signals(df, cfg)`（单个 symbol 的 DataFrame）；
     - `check_contract` 调 `generate_signals(bars_dict, config)` 且参数名必须字面是 `bars`、`config`。
     解法（模板照此写）：
     ```python
     DEFAULT_CONFIG = {
         "symbols": ["<SYM>", ...], "primary_symbol": "<第一个SYM>",
         "fast": 10, "slow": 30, "hold_bars": 12, "size_fraction": 0.01,
     }

     def generate_signals(bars, config):
         # generic harness passes a single-symbol DataFrame; the contract
         # checker passes {symbol: DataFrame}. Accept both.
         if isinstance(bars, dict):
             df = bars[config.get("primary_symbol") or next(iter(bars))]
         else:
             df = bars
         cfg = {**DEFAULT_CONFIG, **(config or {})}
         # toy 双均线：fast 上穿 slow 开多，hold_bars 根后平；返回 Trade 实例
         ...
     ```
     信号体：对 `df["close"]` 算 `rolling(fast).mean()` / `rolling(slow).mean()`，金叉（上一根 fast<=slow、当前 fast>slow）且当前无持仓 → 记 `entry = idx[i]`，`exit = idx[min(i + hold_bars, len(idx) - 1)]`，append `Trade(entry_ts=entry, exit_ts=exit, direction="long", size_fraction=cfg["size_fraction"])`（从 `_shared.run_backtest` import Trade；模板里加 `sys.path` 兜底：`quant_loop_root = Path(__file__).resolve().parents[2]; sys.path.insert(0, str(quant_loop_root))` 仅当 import 失败时）。entry/exit 必须取 `df.index` 的元素本身（保证 `pd.Timestamp` 且在 bar index 上，`validate_trades` :162-178 才过）。
  5. scaffold 自检：生成完文件后，`build_variant` 用 `importlib.util.spec_from_file_location` 加载刚写的 `signals.py`，调 `_shared.templates.strategy_contract_v2.check_contract(module)`（`from _shared.templates.strategy_contract_v2 import check_contract`；new_variant.py 自己先把 quant_loop root 加进 sys.path），打印结果；ContractError 则删除半成品目录并 exit 1。
  6. `test_new_variant.py`（pytest 风格，3 个用例，全部用 `tmp_path` 当 strategies_root，不写真 strategies/）：
     - 生成后 `config.json`/`data_loader.py`/`signals.py` 三文件存在、config 的 `instruments`/`timeframe` 正确；
     - import 生成的 signals.py → `check_contract` 返回 `{"ok": True, ...}`；
     - 同名二次调用 raise（不覆盖）。
  7. 端到端 smoke（任务内手工跑，非 pytest）：`$PY scripts/new_variant.py sprint_smoke_<ts> --timeframe 1h --symbols BTCUSDT` 然后 `$PY -m validation.oos_harness --variant <目录名> --frameworks native --windows 2`，exit code ∈ {0,1}（2 = harness 错误，算失败）。**跑完 `rm -rf` 该 smoke 目录**。

- **验收**：
  ```bash
  cd /Users/mark/multica/quant-loop
  /Users/mark/sdk/mamba-envs/trading/bin/python3 -m pytest scripts/test_new_variant.py -q        # 全绿
  /Users/mark/sdk/mamba-envs/trading/bin/python3 scripts/new_variant.py sprint_smoke_X1 --timeframe 1h --symbols BTCUSDT
  /Users/mark/sdk/mamba-envs/trading/bin/python3 -m validation.oos_harness --variant sprint_smoke_X1_$(date -u +%Y%m%d) --frameworks native --windows 2; echo "exit=$?"
  rm -rf strategies/sprint_smoke_X1_*
  ```
  预期：pytest 全绿；harness `exit=0` 或 `exit=1`；smoke 目录已删除（`ls strategies/ | grep sprint_smoke` 为空）。

- **est**: 30 min | **machine**: mac（数据 parquet 在 mac 上）| **deps**: 无（与 T9 系列文件不相交，可并行）

---

## T11 — CI 脚本 macOS bash 3.2 兼容修复

- **目标**：`validation/ci/validate_changed_variants.sh` 去掉 `mapfile`（bash ≥4 内置，macOS 自带 bash 3.2 没有），改 portable `while read` 收集数组；行为不变。

- **读**：
  - `validation/ci/validate_changed_variants.sh`（46 行；`mapfile` 在 `:15-22`）

- **写**（仅此一个文件）：
  - `validation/ci/validate_changed_variants.sh`

- **步骤**：
  1. 把 `:15-22` 的 `mapfile -t VARIANTS < <(...)` 整块替换为：
     ```bash
     VARIANTS=()
     while IFS= read -r v; do
       VARIANTS+=("$v")
     done < <(
       git diff --name-only "$BASE_REF...$HEAD_REF" -- 'quant-loop/strategies/' \
         | cut -d/ -f3 \
         | sort -u \
         | while read -r v; do
             [ -n "$v" ] && [ -d "strategies/$v" ] && echo "$v"
           done
     )
     ```
     （进程替换 `< <(...)` 和数组 `+=` 在 bash 3.2 都合法。空数组在 `set -u` 下的坑不用管：脚本只在 `${#VARIANTS[@]} > 0` 后才展开 `"${VARIANTS[@]}"`，FAILED 同理。）
  2. 其余行（`set -euo pipefail`、循环、exit 码）一律不动。

- **验收**：
  ```bash
  cd /Users/mark/multica/quant-loop
  bash -n validation/ci/validate_changed_variants.sh && echo SYNTAX_OK
  /bin/bash validation/ci/validate_changed_variants.sh HEAD HEAD; echo "exit=$?"
  ```
  预期：`SYNTAX_OK`；输出含 `[validate] no strategy variant changes between HEAD and HEAD` 且 `exit=0`（`/bin/bash` 是 macOS 的 3.2，`bash --version | head -1` 可确认 3.2.x）。

- **est**: 10 min | **machine**: mac（验收明确要 macOS /bin/bash 3.2）| **deps**: 无

---

## 冲突与注意事项（给 parent）

1. **T9c vs T8（别的片）同文件冲突**：T8 改 `validation/generic_harness.py`（fee shock leg + `:209` 默认窗数）和 `validation/oos_harness.py`（`:194` 默认窗数）+ `validation/test_generic_harness.py`；T9c 也改这三个文件（虽然 hunks 不重叠：T9c 只碰 `:346-354` / `:167-175` 调用点）。**建议 T9c 排在 T8 之后串行执行**，或把调用点接线划给 T8 的片。T9a/T9b 与 T8 文件不相交，可并行。
2. **T9a 的语义涟漪（无需改码，但要知道）**：compute_metrics 的 PF 在传 `trade_pnls` 时变 trade-based 后，以下调用方输出会变化但都属"修正"而非回归：`_shared/templates/run_strategy.py:202-206`、`research/swarm/2026-07-25/` 下若干脚本（out of scope，不碰）。`validation/metrics.py` 的 `metrics_from_run` PF 走自己的 `profit_factor()`（:117），不变。若 T2（fee_shock.py）内部调 compute_metrics 并传 trade_pnls，其 PF 也会是 trade 口径——与 G4 一致，正是目标。
3. **T9b 向后兼容**：`window_vectorbt` 默认 None，T9c 落地前两个 harness 行为完全不变，T9b/T9c 之间的窗口期不会 break 任何测试。
4. **T10 与 T13（onboarding 文档）**：T13 依赖 T10 的 CLI 形态，T10 里 CLI 参数名（`--timeframe`/`--symbols`/`--strategies-root`）定了就别再改。
5. **数据事实（T10 依赖）**：`quant-loop/data/` 只有 perp_1m/5m/15m/30m/2h 且无 1h 目录，1m 仅 BTC/ETH/SOL——scaffold 的 resample 回退不是可选项，是验收跑通的必要条件。
