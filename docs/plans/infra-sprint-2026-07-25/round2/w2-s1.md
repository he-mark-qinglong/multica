# 片 w2-s1 — W2/T1+T2 细化：strict gate 落地（gate.go + gate_test.go）

> Round-2 执行卡片 · 2026-07-25
> 执行者画像：caocao-m3 agent，零上下文，30 min 预算，只有本卡片可读。
> 本切片只做两件事：把 `server/internal/gate/gate.go` 换成 strict 语义（T1），
> 把 `server/internal/gate/gate_test.go` 改写+合并到新语义（T2）。
> **不改其他任何文件。**（metric.go 注释、迁移、前端属于别的切片。）

## 0. 背景（自包含，读完即可开工）

现状 bug：`server/internal/gate/gate.go:115-117` 在指标缺失时 `res.Pass = true; res.Note = skipNote`
（"skipped: no data"），且 `:128-135` 的 overall 判定是「没有 evaluated 规则失败 + sharpe 存在 → pass」。
结果：只上传了 sharpe 的策略（生产实例 vpvr_stable_depeg_p3opt_091，sharpe=31.7）整体 PASS——虚过。

修复蓝本已存在（未经生产落地）：
- 代码蓝本：`quant-loop/research/swarm/2026-07-25/gate-ledger-fix/gate_proposal.go`（157 行，完整可编译的替换文件）
- 测试蓝本：`quant-loop/research/swarm/2026-07-25/gate-ledger-fix/gate_test_proposal.go`（135 行，6 个新测试）

新语义一句话：**全部 6 条规则 required，缺失即 FAIL；唯一例外是 sharpe 缺失 → overall 状态 `no-data`（第三态）**。
overall 状态集合从 `{"pass","fail",""}` 变为 `{"pass","fail","no-data"}`，`""` 不再被返回。

### 0.1 proposal vs 现 gate.go 的 diff 要点（T1 的改动清单）

| 位置（现文件） | 现内容 | proposal 内容 |
|---|---|---|
| `gate.go:6-10` 包文档注释 | P1 hardcoded 说明 | 换成新语义说明（见 T1 步骤 3 的注释草案） |
| `gate.go:17-20` 状态常量 | `StatusPass`/`StatusFail` 两个 | 增加 `StatusNoData = "no-data"`（proposal:22-26） |
| `gate.go:22-25` skipNote | 单个 `const skipNote` | 改成 const 块：`skipNote`（保留，仅为前向兼容，当前无 optional 规则）+ 新增 `missingNote = "missing required metric"`（proposal:28-35） |
| `gate.go:53-58` `rule` 结构体 | 4 个字段 | 增加第 5 个字段 `required bool`（proposal:62-68） |
| `gate.go:77-97` `DefaultRules` | 每条规则 4 个元素 | 每条末尾加 `, true`（6 条全 required，proposal:88-108） |
| `gate.go:109-136` `Evaluate` | skip 即 pass；`m.Sharpe != nil → pass`；否则 `""` | 增加 `sharpeMissing` 追踪：required 缺失 → `res.Pass=false; res.Note=missingNote`（sharpe 缺失时置 `sharpeMissing=true`）；末尾 switch 改为：`failed && sharpeMissing → no-data` / `failed → fail` / `sharpeMissing → no-data` / 默认 `pass`（proposal:118-157） |

未变部分：`Metrics`、`RuleResult`（JSON tag 不动——前端按此形状渲染，见 `gate_test.go` 的 JSON pin 测试）、`compare` 函数、max_drawdown 的 `math.Abs` 幅度比较、`oos_windows` 的 int32→float64 转换。

### 0.2 关键语义边界（写测试前必须理解）

- `Evaluate(Metrics{})`（全空）→ 6 条全 missing，`failed=true` 且 `sharpeMissing=true` → **`no-data`**。
- sharpe 缺失 + 其他某条 evaluated 失败 → 仍 **`no-data`**（proposal switch 第一条优先）。这是有意的：没有 sharpe 就没有判决基础。
- sharpe 存在但其他任何 required 缺失 → **`fail`**（这就是 vpvr_stable_depeg_p3opt_091 修复后的预期行为）。
- detail 数组永远 6 条、按 `DefaultRules` 顺序；missing required 的条目是 `{actual: null, pass: false, note: "missing required metric"}`。

### 0.3 环境事实（已核实，直接信任）

- Go 1.26.1 darwin/arm64 在 mac 本机可用；`cd /Users/mark/multica/server && go test ./internal/gate/ -count=1` 基线**当前全绿**（改动前）。
- 测试与实现同包（`package gate`），所以测试可直接引用未导出的 `missingNote`、`skipNote`。
- 现 `gate_test.go` 顶部已有 helper：`f64(v float64) *float64`、`i32(v int32) *int32`、`ruleByName(t, detail, name) RuleResult`（gate_test.go:8-20）。proposal 测试文件自带同名 helper——合并时**保留现有的一份即可，不要复制第二份**（重复定义编译失败）。

---

## T1 — strict gate 落地：gate.go 替换为 proposal 语义

- **Goal（一句话）**：消除 skip-pass 漏洞——6 条规则全 required、缺失即 fail、新增 `no-data` 第三态。
- **Reads**：
  - `/Users/mark/multica/server/internal/gate/gate.go`（136 行，被改对象）
  - `/Users/mark/multica/quant-loop/research/swarm/2026-07-25/gate-ledger-fix/gate_proposal.go`（157 行，蓝本）
- **Writes**：`/Users/mark/multica/server/internal/gate/gate.go`（唯一改动文件）
- **Dependencies**：无。
- **Machine**：either（mac 已验证 go1.26.1；.105 亦有 Go）
- **Est**：15 min

### 步骤

1. 通读 `gate_proposal.go` 全文。它就是替换后的 gate.go，**唯一不能照抄的是文件头的包文档注释**（proposal:6-15 是 PROPOSAL 说明，落地时必须去掉）。
2. 用 proposal 全文覆盖 `server/internal/gate/gate.go`，但把 proposal 的第 1-16 行（包文档注释 + `package gate`）替换为下面这段（保留现文件 gate.go:6-10 的 P2 说明、更新语义描述）：

```go
// Package gate evaluates trading-strategy hard gates against parsed run
// metrics so overfit / below-bar candidates are flagged automatically at
// ingest time (an in-sample Sharpe of 5.7 with OOS Sharpe 0.6 must fail
// loudly).
//
// The rule set is HARDCODED for now (P1.5). Per-workspace / per-campaign
// configuration is planned for P2 — when it lands, DefaultRules becomes the
// fallback and Evaluate gains a rules parameter. Keep the rule semantics
// stable: the frontend renders gate_detail entries straight from this
// package's JSON shape.
//
// Strict semantics (gate-ledger-fix, 2026-07-25): every DefaultRules metric
// is REQUIRED — an absent metric fails its rule instead of skipping. The one
// exception is sharpe: without it there is no basis to judge, so the overall
// status is "no-data" instead of "fail".
package gate
```

3. 确认 §0.1 表格中的每一处 diff 都落进去了（逐条对照，不要只覆盖后不管）。
4. **不要**顺手改 `server/internal/handler/metric.go`（persistGate 对 `status == ""` 的跳过逻辑在新语义下天然不触发、无需改动；其过时注释由另一个切片处理）。`Evaluate` 现在永不返回 `""`，调用方安全。
5. 自查不变量（grep）：

```bash
cd /Users/mark/multica/server
grep -n 'StatusNoData = "no-data"' internal/gate/gate.go        # 期望 1 行
grep -n 'missingNote = "missing required metric"' internal/gate/gate.go  # 期望 1 行
grep -c ', true}' internal/gate/gate.go                          # 期望 6（6 条规则全 required）
grep -n 'sharpeMissing' internal/gate/gate.go                    # 期望 ≥3 行（声明、置位、switch 两处）
```

### 验收（runnable）

```bash
cd /Users/mark/multica/server && go build ./internal/gate/ && go vet ./internal/gate/ && echo BUILD_OK
```

期望输出 `BUILD_OK`，无编译/vet 错误。
**注意**：此卡片完成后 `go test ./internal/gate/` 会红（旧测试断言旧语义）——这是**预期**的，由 T2 修绿。本卡片验收只看 build+vet+上面的 grep。

---

## T2 — gate 测试改写与合并（5 个旧测试重写 + 4 个新测试合入）

- **Goal（一句话）**：让 `gate_test.go` 全部断言新 strict 语义，`go test` 全绿。
- **Reads**：
  - `/Users/mark/multica/server/internal/gate/gate_test.go`（234 行，被改对象）
  - `/Users/mark/multica/quant-loop/research/swarm/2026-07-25/gate-ledger-fix/gate_test_proposal.go`（135 行，新测试蓝本）
  - `/Users/mark/multica/server/internal/gate/gate.go`（T1 落地后的新语义，必要时对照常量名）
- **Writes**：`/Users/mark/multica/server/internal/gate/gate_test.go`（唯一改动文件）
- **Dependencies**：**T1 必须先完成**（测试引用 `StatusNoData`/`missingNote`/`skipNote` 常量）。
- **Machine**：either
- **Est**：25 min

### 总览：最终 12 个测试

| 处理 | 测试名 | 来源 |
|---|---|---|
| 保留不动 ×3 | `TestEvaluateTypicalPass`（:23-49）、`TestEvaluateOverfitFailsOnOOS`（:51-76）、`TestEvaluateAnyFailureDominates`（:188-201） | 现有；输入字段齐全，新语义下断言仍成立 |
| 重写 ×5 | 见下（含 round1 漏报的第 5 个） | 现有改造 |
| 新增 ×4 | 见下 | proposal 搬入 |
| **不搬** ×2 | `TestEvaluateStrongCandidateStillPasses`（与 `TestEvaluateTypicalPass` 输入完全相同，重复）、proposal 的 `TestEvaluateMissingSharpeIsNoData`（被下面的重写版覆盖，同名冲突） | proposal:100-117、72-81 |

### ⚠️ round1 漏报：冲突旧测试是 5 个，不是 4 个

round1 只列了 4 个；实际 `TestRuleResultJSONShape`（gate_test.go:203-234）后半段也断言 skip 语义（`note == "skipped: no data"`、`actual == nil` 但隐含 `pass == true`），新语义下同样必挂。合计 5 个。

### 步骤 1：保留区不动

`f64`/`i32`/`ruleByName` helper（:8-20）、`TestEvaluateTypicalPass`、`TestEvaluateOverfitFailsOnOOS`、`TestEvaluateAnyFailureDominates` 原样保留。

### 步骤 2：重写 5 个旧测试

**(a) `TestEvaluateMissingOOSDataSkippedVisible`（:78-111）→ 改名 `TestEvaluateMissingOOSDataNowFails`**

核心翻转：OOS 缺失从「skip 但仍 pass」变为「missing required → fail」。参考实现：

```go
func TestEvaluateMissingOOSDataNowFails(t *testing.T) {
	// No OOS data at all: OOS rules are missing-required — visible with
	// actual=null, pass=false, note "missing required metric" — and they
	// FAIL the gate even though every in-sample rule passes.
	status, detail := Evaluate(Metrics{
		Sharpe:       f64(1.5),
		AnnReturn:    f64(0.30),
		MaxDrawdown:  f64(0.10),
		ProfitFactor: f64(2.0),
	})
	if status != StatusFail {
		t.Fatalf("status = %q, want fail", status)
	}
	for _, name := range []string{"oos_windows", "oos_sharpe"} {
		r := ruleByName(t, detail, name)
		if r.Actual != nil || r.Pass || r.Note != missingNote {
			t.Errorf("missing required OOS rule malformed: %+v", r)
		}
	}
	// Partial OOS data: present-but-failing rule fails on value; absent
	// rule fails as missing-required.
	status, detail = Evaluate(Metrics{
		Sharpe:       f64(1.5),
		AnnReturn:    f64(0.30),
		MaxDrawdown:  f64(0.10),
		ProfitFactor: f64(2.0),
		OOSSharpe:    f64(0.4), // present and below the >= 1.0 bar
	})
	if status != StatusFail {
		t.Fatalf("partial OOS: status = %q, want fail", status)
	}
	if r := ruleByName(t, detail, "oos_sharpe"); r.Pass || r.Note != "" {
		t.Errorf("oos_sharpe present-but-failing must fail on value: %+v", r)
	}
	if r := ruleByName(t, detail, "oos_windows"); r.Pass || r.Note != missingNote {
		t.Errorf("oos_windows absent must fail as missing required: %+v", r)
	}
}
```

**(b) `TestEvaluateMissingSharpeStatusNull`（:113-134）→ 改名 `TestEvaluateMissingSharpeIsNoData`**

```go
func TestEvaluateMissingSharpeIsNoData(t *testing.T) {
	// Everything but sharpe present and passing → no-data: without sharpe
	// there is no basis to judge. The sharpe rule itself is missing-required.
	status, detail := Evaluate(Metrics{
		AnnReturn:    f64(0.30),
		MaxDrawdown:  f64(0.10),
		ProfitFactor: f64(2.0),
		OOSSharpe:    f64(1.2),
		OOSWindows:   i32(4),
	})
	if status != StatusNoData {
		t.Fatalf("status = %q, want no-data", status)
	}
	r := ruleByName(t, detail, "sharpe")
	if r.Actual != nil || r.Pass || r.Note != missingNote {
		t.Errorf("missing sharpe should be missing-required: %+v", r)
	}

	// No usable metrics at all → no-data as well (every rule missing,
	// sharpe missing dominates).
	if status, _ := Evaluate(Metrics{}); status != StatusNoData {
		t.Fatalf("empty metrics: status = %q, want no-data", status)
	}
}
```

注意：与旧版相比，输入**补上了** `OOSSharpe`/`OOSWindows`——否则其他规则也 missing，虽然 status 仍是 no-data，但「everything but sharpe passing」的语义就立不住了。

**(c) `TestEvaluateDrawdownMagnitudeSignConvention`（:136-155）→ 保留名字，补全字段**

旧版用 `Metrics{Sharpe, MaxDrawdown}` 两字段断言 pass——新语义下其他 4 条 missing 必 fail。改法：先造一个全字段基准，再只变 MaxDrawdown：

```go
func TestEvaluateDrawdownMagnitudeSignConvention(t *testing.T) {
	base := func(v float64) Metrics {
		return Metrics{
			Sharpe: f64(1.5), AnnReturn: f64(0.30), MaxDrawdown: f64(v),
			ProfitFactor: f64(2.0), OOSSharpe: f64(1.2), OOSWindows: i32(4),
		}
	}
	// 0.20 and -0.20 are the same 20% drawdown: both pass the < 0.25 bar.
	for _, v := range []float64{0.20, -0.20} {
		status, detail := Evaluate(base(v))
		if status != StatusPass {
			t.Fatalf("mdd %v: status = %q, want pass", v, status)
		}
		r := ruleByName(t, detail, "max_drawdown")
		if r.Actual == nil || *r.Actual != 0.20 {
			t.Errorf("mdd %v: detail actual should be magnitude 0.20: %+v", v, r)
		}
	}
	// 0.30 and -0.30 both fail.
	for _, v := range []float64{0.30, -0.30} {
		if status, _ := Evaluate(base(v)); status != StatusFail {
			t.Fatalf("mdd %v: status = %q, want fail", v, status)
		}
	}
}
```

**(d) `TestEvaluateBoundaryValues`（:157-186）→ 保留名字，每个子用例补全字段**

旧版 5 个子用例全是字段残缺的 `Metrics`，新语义下全因 missing 而 fail，pass 断言全挂。改法：引入全字段基准 helper（可复用 (c) 的 `base` 思路，或写成包内测试 helper `passingMetrics()`），每个子用例只覆盖被测字段。骨架：

```go
func TestEvaluateBoundaryValues(t *testing.T) {
	passing := func() Metrics {
		return Metrics{
			Sharpe: f64(1.5), AnnReturn: f64(0.30), MaxDrawdown: f64(0.10),
			ProfitFactor: f64(2.0), OOSSharpe: f64(1.2), OOSWindows: i32(4),
		}
	}
	// Sharpe exactly at the >= 1.0 bar passes.
	m := passing()
	m.Sharpe = f64(1.0)
	status, detail := Evaluate(m)
	if status != StatusPass {
		t.Fatalf("sharpe 1.0: status = %q, want pass", status)
	}
	if r := ruleByName(t, detail, "sharpe"); !r.Pass {
		t.Errorf("sharpe 1.0 must pass >= 1.0: %+v", r)
	}
	// Just below fails.
	m = passing()
	m.Sharpe = f64(0.9999)
	if status, _ := Evaluate(m); status != StatusFail {
		t.Fatalf("sharpe 0.9999: status = %q, want fail", status)
	}
	// profit_factor is strictly greater: exactly 1.5 fails.
	m = passing()
	m.ProfitFactor = f64(1.5)
	if status, _ := Evaluate(m); status != StatusFail {
		t.Fatalf("pf 1.5: status = %q, want fail (> 1.5 is strict)", status)
	}
	// max_drawdown exactly 0.25 fails (< 0.25 is strict).
	m = passing()
	m.MaxDrawdown = f64(0.25)
	if status, _ := Evaluate(m); status != StatusFail {
		t.Fatalf("mdd 0.25: status = %q, want fail (< 0.25 is strict)", status)
	}
	// ann_return exactly 0.15 passes; oos_windows exactly 3 passes;
	// oos_sharpe exactly 1.0 passes.
	m = passing()
	m.AnnReturn = f64(0.15)
	m.OOSWindows = i32(3)
	m.OOSSharpe = f64(1.0)
	if status, _ := Evaluate(m); status != StatusPass {
		t.Fatalf("boundary ann_return/oos: status = %q, want pass", status)
	}
}
```

**(e) `TestRuleResultJSONShape`（:203-234）→ 保留名字，只改后半段**

前半段（:205-221，evaluated sharpe 条目的 key 齐全 + 省略 note）不动。后半段（:223-233）原来断言 oos_sharpe 被 skip；改成断言 missing-required 的线格式（这一步和新增测试 `TestRuleResultJSONShapeForMissingRequired` 有重叠，但此处同时 pin 了 `pass:false`，保留）：

```go
	// Missing-required rule: explicit null actual, pass=false, note present.
	raw, _ = json.Marshal(ruleByName(t, detail, "oos_sharpe"))
	if err := json.Unmarshal(raw, &obj); err != nil {
		t.Fatal(err)
	}
	if v, ok := obj["actual"]; !ok || v != nil {
		t.Errorf("missing required rule actual should be explicit null: %s", raw)
	}
	if obj["pass"] != false {
		t.Errorf("missing required rule pass should be false: %s", raw)
	}
	if obj["note"] != missingNote {
		t.Errorf("missing required rule note: %s", raw)
	}
```

（`Evaluate(Metrics{Sharpe: f64(1.2)})` 的调用不变：sharpe evaluated，oos_sharpe missing-required。）

### 步骤 3：从 proposal 搬入 4 个新测试（逐字复制即可）

从 `gate_test_proposal.go` 复制以下 4 个函数（原文已验证与新语义一致）：
- `TestEvaluateMissingCoreFieldsNowFail`（proposal:25-40）——sharpe=31.7 孤值 → fail（vpvr 回归测试）
- `TestEvaluateProfitFactorRequired`（proposal:42-70）——PF 缺失 → fail；PF=1.22 <1.5 → fail
- `TestEvaluateOOSWindowsAtLeastThree`（proposal:83-98）——OOSWindows=2 → fail
- `TestRuleResultJSONShapeForMissingRequired`（proposal:119-135）——ann_return missing 的 JSON 形状

**不要**复制 proposal 顶部的 `f64`/`i32`/`ruleByName`（重复定义）。**不要**复制 `TestEvaluateStrongCandidateStillPasses`（与现有 `TestEvaluateTypicalPass` 重复）和 proposal 版 `TestEvaluateMissingSharpeIsNoData`（与步骤 2(b) 同名冲突）。

### 步骤 4：自查

```bash
cd /Users/mark/multica/server
grep -c '^func Test' internal/gate/gate_test.go          # 期望 12
grep -n 'skipped: no data' internal/gate/gate_test.go    # 期望 0 行（旧 skip 断言全部清除；若用 skipNote 常量名引用则为 0 次出现）
grep -n '""' internal/gate/gate_test.go | grep -i 'want\|status'  # 期望 0 行（不再断言空状态）
```

### 验收（runnable）

```bash
cd /Users/mark/multica/server && go test ./internal/gate/ -count=1 -v 2>&1 | tee /tmp/gate_test_out.txt; \
  grep -c '^--- PASS' /tmp/gate_test_out.txt && ! grep -q '^--- FAIL\|^FAIL' /tmp/gate_test_out.txt && echo ALL_GREEN
```

期望：`--- PASS` 计数 = 12，输出 `ALL_GREEN`。再加一道全仓保险：

```bash
cd /Users/mark/multica/server && go build ./... && go vet ./internal/gate/
```

（`go build ./...` 确认没有其他包因 gate 常量/行为变化编译失败；`Evaluate` 签名未变，预期安全。）

---

## 切片级备注

- **与其他切片的文件冲突**：本切片只碰 `server/internal/gate/{gate.go,gate_test.go}`。W2 的 T3/T5（metric.go）、T5（migrations）、T6-T9（前端）文件互不相交，可并行。唯一语义耦合：前端 T8 渲染 `gate_detail` 依赖 `RuleResult` 的 JSON 形状（`rule/op/threshold/actual/pass/note`）——本切片**不改该形状**（两个 JSON pin 测试就是护栏）。
- **遗留事项（不在本切片）**：`metric.go:224-226` persistGate 注释、`metric.go:323-327` RunMetricResponse 注释仍写 `"pass"|"fail"|null` 旧语义，由 T3/T5 所在切片同步；DB CHECK 约束（迁移 125）由 T5 所在切片处理。
- **已知接受的行为变化**：sharpe 缺失 + 其他规则 evaluated 失败 → `no-data`（不是 fail）。这是 proposal 的有意语义，测试 (b) 已 pin。
