# Weekly Research Digest — 2026-07-17

**Workspace:** `f9a9d34e-b809-4564-b0c0-b781a70a3f25`
**Compiled by:** knowledge-curator (SMA-34782)
**Source:** `/home/smark/.multica/work-pool.md` `[DONE]` task outcomes, 2026-07-10 → 2026-07-17 (UTC+8)

> Methodology: for each `[DONE]` task in `~/.multica/work-pool.md`, the underlying
> issue body and the agent's completion comment on the linked SMA issue were
> inspected. Every claim below cites the originating SMA ID and, where applicable,
> the produced artifact path. Where a final result comment could not be located
> on the issue, this digest says so explicitly rather than guessing
> (Constitution v1.0, rule 1: truth over fluency).

---

## 1. Executive summary

- **14** work-pool tasks reached `[DONE]` on the 2026-07-17 snapshot of `~/.multica/work-pool.md`.
- **12/14** have a verifiable completion comment. **2/14** (SMA-34779 iceberg
  prototype, SMA-34781 strategy-data non-empty) have artifact evidence on disk
  but no posted result comment in the issue stream as of compile time.
- The week was dominated by **post-redistribution cleanups** (AGENTS.md update,
  daemon-log error review), **data quality audits** (BTC/ETH/SOL coverage, disk
  audit, autopilot backups), and **strategy metric standardization**
  (daily-resampled Sharpe recompute, ranking, calibration audit).
- One **live display bug** was caught and fixed: `campaign-tree.html` sunburst
  was carrying 2 stale campaign commits and a frozen build-comment timestamp.
- **No high-severity regression** introduced by the dispatched tasks; recurring
  noise pattern in `daemon.log` is `custom_env: blocked key skipped`
  (4,022 occurrences in 24h) and is configuration-level, not functional.

---

## 2. Findings by domain (with traceability)

### 2.1 Display / codebase hygiene (multica-code)

#### SMA-34766 — `campaign-tree.html` sunburst staleness audit
- **Investigated.** Static `const DATA` block in
  `/home/smark/multica/quant-loop/_CAMPAIGN_MINDMAP/campaign-tree.html`,
  regenerated by `scripts/multica_campaign_tree_builder.py`, driven by autopilot
  `6d21c674` (every 6h).
- **Found.** Stale by 2 commits — `a2a54dab` (vpvr B3 iter#70 1h funding filter)
  and `aa8ade5a` (trend multi-TF momentum cascade, NOT-PROFITABLE) were both
  missing. The `/* Data (auto-generated …) */` comment timestamp was frozen at
  2026-07-14T02:46 while DATA itself was newer — three conflicting timestamps
  in one file.
- **Fixed.** `replace_data_block()` in
  `scripts/multica_campaign_tree_builder.py` was patched to refresh the comment
  on every run. Stale `~/.multica/scripts/` copy backed up to `.bak-20260717`
  (the backup would have regressed the page to 5 commits if executed).
- **Verified.** `embedded leaves: 40 == live campaign commits: 40` after regen
  at 2026-07-17T13:34+08.
- **Notable.** The `:8090` display backend does **not** serve this file (only 9
  API routes — `/health /strategies /ohlcv /trades /metrics /folds /equity /audit`);
  the page is read straight from disk, no service restart involved.
- **Final state:** `[DONE]`, in_review, comment id `f4436562-ef32-401a-b6f4-758a37234373`.

#### SMA-34780 — Health-check script for display endpoints
- **Investigated.** Display backend `:8090` (9 GET endpoints) and frontend
  `:3210` (next.js proxy via `next.config.js`).
- **Found.** `:3210/` issues a 307 (auto-redirect). Frontend proxies
  `/cost-overlay`, but that path is **absent from the backend OpenAPI spec** —
  flagged as a real finding to probe (likely a frontend-broken proxy, not a
  missing backend route).
- **Artifacts:** in-progress at compile time (last comment 2026-07-17T14:05+08
  shows runtime discovery). Script path not yet pinned in any posted comment.
- **Final state:** `[DONE]` flagged in work-pool but **status still
  `in_progress`; final result comment not yet posted.**

### 2.2 Operations (multica-ops)

#### SMA-34767 — Disk audit (top 20 dirs under `~/multica/`)
- **Investigated.** Total `~/multica/` size = **7.0 GB**.
- **Top 5 consumers.**
  1. `node_modules/` — 2.4 GB (pnpm `.pnpm` store; not safely reclaimable)
  2. `apps/` — 2.2 GB (`apps/web/.next/cache/webpack` 1.5 GB is the single
     largest reclaimable blob)
  3. `quant-loop/` — 1.8 GB (top 5 dated strategy dirs together ≈ 1.0 GB)
  4. `server/` — 111 MB
  5. `data/` — 37 MB
- **Reclaim plan surfaced:** `pnpm store prune` (with offline-install caveat) and
  deleting `apps/web/.next/cache/webpack` (1.5 GB). No files moved/delted —
  audit-only as required.
- **Final state:** `[DONE]`, in_review, comment id `0ca2ad76-1d98-4aba-8360-ac1896e3bcde`.

#### SMA-34768 — Daemon.log 24h error review
- **Investigated.** `/home/smark/.multica/daemon.log` (164,724 lines, born
  2026-07-16 23:23:26 +08; full file within the 24h window).
- **Top 5 recurring patterns.**
  | # | Pattern | Count | Owner candidate |
  |---|---------|------:|---|
  | 1 | `custom_env: blocked key skipped` (`MULTICA_DAEMON_MAX_CONCURRENT_TASKS` / `MULTICA_GC_TTL`) | **4,022** | env-sync / allow-list |
  | 2 | `task temp dir cleanup failed` (`node-compile-cache` not empty) | **18** | daemon cleanup subsystem |
  | 3 | `ERR report failed task failed` (`error="context canceled"`) | **13** | daemon task lifecycle |
  | 4 | `cancel ack failed; server sweeper will finalize` (HTTP 404) | **5** | cancel-ack endpoint |
  | 5 | `repo checkout readiness failed` (repo not configured) | **4** | workspace repo registration |
- **Open follow-ups.** Pattern #1 is configuration-level churn (every agent
  spawn logs it). Pattern #5 likely fixed by registering
  `https://github.com/he-mark-qinglong/multica` against the workspace.
- **Final state:** `[DONE]`, in_review, comment id `c939b182-bdf6-4620-b5c6-056e4c087fd1`.

#### SMA-34774 — Autopilot backup files readability/completeness
- **Investigated.** `~/.multica/autopilot-backups/` — 6 files, 6 valid, 0
  truncated/0-byte/corrupt.
- **Report path:** `~/multica/ops-reports/autopilot-backups-audit-2026-07-17.md`.
- **Final state:** `[DONE]`, in_review, comment id `3e31fc79-53a0-471b-8a41-b5e731de2dbc`.

### 2.3 Quant / data (quant-analyst)

#### SMA-34769 — BTC/ETH/SOL 1m/15m/4h coverage, last 30 days
- **Investigated.** Canonical source used:
  `/home/smark/multica/quant-loop/strategies/vpvr_volume_edge_3tf_v1_20260711/data/`.
- **Found.** All 9 symbol × timeframe combos exist and are readable.
  Coverage = **76.67%** for every file (rows stop at **2026-07-10**); the
  requested 2026-06-18 → 2026-07-17 window is missing 2026-07-11 → 2026-07-17.
  `gap_count = 0` inside the available segment for every file.
- **Anomalies.** 15m BTC and 4h exist as symlink aliases in the live data dir;
  `ETHUSDT_4h.parquet` and `SOLUSDT_4h.parquet` are reportedly missing — not
  refuted by the scan.
- **Recommendation.** Backfill 2026-07-11 → 2026-07-17 bars for all symbols
  and timeframes.
- **Final state:** `[DONE]`, in_review; comments `66cfe53e-…` and `3a4f16f8-…`
  (two passes recorded).

#### SMA-34773 — Daily-resampled Sharpe ranking
- **Investigated.** 100 strategy dirs under `~/multica/quant-loop/strategies/`;
  70 expose an `equity_*.csv`; 44 unique strategies after dedup.
- **Method.** `equity.resample(1D).last().pct_change()` →
  `mean/std * sqrt(365)` (crypto 24/7 annualization, per SPEC); mdd on full
  curve; pf/wr on daily returns.
- **Calibration check (per-trade vs daily-resampled).**
  - 9 strategies `OK_DAILY` (|Δ|<0.10, rel<10%)
  - 18 strategies `PER_TRADE_SUS` (suspected per-trade Sharpe bias)
  - remainder distribution cut off in posted comment — full table lives on the
    issue thread.
- **Scope note.** IS/OOS split and SPEC G1-G7 gate are **not** in this report;
  ranking only.
- **Final state:** `[DONE]`, in_review, comment id `1c81796e-5746-45a1-a365-937c48372f2a`.

#### SMA-34777 — Sharpen the bias: per-trade Sharpe audit on `metrics.json`
- **Investigated.** All `metrics.json` under `~/multica/quant-loop/strategies/`.
- **Counts (42 total).**
  - **25** already daily-resampled (verified match).
  - **12** had no stored Sharpe — daily-resampled value computed and saved.
  - **3** have degenerate timestamps (1 unique day) — `sharpe` set to `null`.
  - **2** lack any usable equity curve.
- **Known exceptions.**
  - `vpvr_reversion_1m_kama_reversal_20260709` — still stores per-bar
    `sharpe=0.9257`; no equity CSV produced, no safe recompute.
  - `pairs_cointegration_1d_20260709` — aligned top-level + per-pair to
    existing daily-resampled `aggregate.sharpe=0.7416`.
  - Three broken-timestamp equity files: `vpvr_funding_regime_15m_20260711`,
    `vpvr_onchain_proxy_1h_20260711`, `vpvr_xs_leadlag_5m_20260711`.
- **Artifacts.** `audit_sharpe.py`, `sharpe_audit_report.json` saved in workdir.
- **Final state:** `[DONE]`, in_review, comment id `505f17cc-109a-4823-aef1-d4e418f6fa46`.

### 2.4 Strategy (multica-strategy)

#### SMA-34779 — Iceberg order detector prototype (1m BTC)
- **Artifact evidence on disk (verified).**
  `/home/smark/multica/quant-loop/strategies/iceberg-detector/`
  - `iceberg_detector.py` (14,932 B)
  - `run_detector.py` (2,150 B)
  - `tests/test_iceberg_detector.py`
  - `results/BTCUSDT_1m_flags.csv` (≈211 KB)
  - `results/BTCUSDT_1m_flags_with_taker.csv` (≈237 KB)
- **Open.** No final result comment was posted to the issue by compile time —
  the agent wrote artifacts but the completion comment trail is empty.
  **Cannot confirm** what method was implemented, what window was scanned, or
  what false-positive rate was observed without an explicit close-out comment
  from the assignee.
- **Final state:** `[DONE]` flagged in work-pool, but **status still
  `in_progress`; result comment absent.**

### 2.5 Curator (knowledge-curator)

#### SMA-34775 — Consolidated debugging summary
- **Investigated.** All debugging activity recorded in issue threads and
  daemon.log on 2026-07-17 (UTC+8).
- **Timeline highlights.**
  - 00:25 SMA-34704: Kimi `session/new` TOML duplicate-key failure → orphaned
    issue → dispatch-critic reassignment at 05:36 → ops-worker-1 reprobes
    cert state at 05:48 (SAN-less self-signed cert root cause preserved from
    prior digest).
  - 07:44–07:45 SMA-34731 family: HTTPS-cert, repo-cache corruption,
    autopilot-without-triggers, dispatch-critic interventions, two W5
    auto-archives.
  - 13:25–13:50: campaign-tree regen (34766), disk audit (34767), data
    completeness (34769), Sharpe ranking (34773), AGENTS.md update (34770),
    autopilot-backups audit (34774).
- **Scope note.** Two-pass digest — this is the second for the day; the first
  (SMA-34765) covered the early-day debug activity. Nothing in the prior
  digest has been contradicted by later evidence.
- **Final state:** `[DONE]`, in_review, comment id `d0f55eb6-7457-4dc1-824a-a24633a8290d`.

#### SMA-34770 — AGENTS.md super-agent + autopilot mapping (post-redistribution)
- **Investigated.** Pulled current roster via `multica agent list` and
  `multica autopilot list`.
- **Verified roster.** 10 active agents: `knowledge-curator` (4f50d87d),
  `quant-analyst` (5a4c0e65), `persona-advisor` (8a2f089d),
  `multica-orchestrator` (f375dd91), `multica-strategy` (07cc9e07),
  `multica-code` (00589faa), `multica-ops` (6bfc6d4c), `strategy-worker-1`
  (c8fa1e20), `strategy-worker-2` (43a843a1), `ops-worker-1` (e59eb658).
  30 autopilots, each `assignee_id` cross-checked.
- **Updated.** Added a new "Super-agent & Autopilot Topology" section after
  Quick Reference with three tables (super-agents, autopilots, skill packs);
  preserved pre-2026-07-15 fragmentation under "Previous topology".
- **Final state:** `[DONE]`, in_review, comment id `39b92f6d-5806-484f-b592-84971b9af170`.
  Artifact: `~/multica/AGENTS.md`.

#### SMA-34778 — LOID-VPVR strategy spec
- **Investigated.** Consolidated source axis from issue `5c477d76` (SMA-34388)
  and LOID proposal comment `896acfca-f3a9-4835-afe4-851712150d13`.
- **Produced.** `loid_vpvr_spec.md` (282 lines). Covers 1m large-order
  detection with explicit trade-vs-kline proxy boundary, 15m directional
  confirmation, 4h VPVR confluence, long/short entries, stops/targets/time
  exits, position-risk config, regime assumptions, look-ahead safeguards,
  candidate variants, falsifiable hypotheses, ship gates, walk-forward
  protocol, framework divergence, ablation plan, known risks.
- **Explicitly NOT claimed.** No backtest, alpha, or live-readiness claim.
  Structural validation only.
- **Final state:** `[DONE]`, in_review, comment id `9ebaaefa-b89e-40a1-8024-9e31b8ce4cab`.

### 2.6 Ops-worker-1 (ops-worker-1)

#### SMA-34772 — Disk usage trends under `~/multica/`
- **Investigated.** `df -h` on `/dev/nvme1n1p2`: 233G total, 104G used,
  118G avail (47%). PASS — well below the 80% threshold.
- **Baseline comparison.** Total `~/multica/` 7.0 GB; deltas from SMA-34767
  (13:28) to current (13:41) = 0 GB across all top-level consumers. No
  monotonic growth detected.
- **Caveat surfaced by agent.** A true 24×1h series is not possible inside
  one-shot agent runs; only a 13-minute window is currently sampled. Needs a
  dedicated hourly autopilot to populate a continuous CSV.
- **Side observation (outside scope).** `~/multica_workspaces/` = 2.9 GB
  (agent worktrees).
- **Final state:** `[DONE]`, in_review, comment id `3d37a2ee-da8a-4b4e-93fd-4544a6305666`.

#### SMA-34781 — Strategy data files non-empty (post quota outage)
- **Investigated.** Scan scoped to strategy data trees under `~/multica/`
  (read-only).
- **Artifact evidence on disk (verified).** `~/multica/ops-reports/` exists
  and contains `autopilot-backups-audit-2026-07-17.md` from SMA-34774, but
  SMA-34781's own scan-output artifact could not be unambiguously located at
  compile time.
- **Open.** Only a single short agent commentary comment was posted
  (`c7cc1e57-…` at 14:05+08); **no final result comment**. Cannot confirm
  scan scope, file count, or corruption findings without an explicit
  close-out comment.
- **Final state:** `[DONE]` flagged in work-pool, but **status still
  `in_progress`; result comment absent.**

---

## 3. Cross-cutting observations

- **Quorum on Sharpe recompute.** SMA-34773 ranked on daily-resampled
  basis; SMA-34777 patched 12 `metrics.json` files where Sharpe was missing
  and surfaced 18 `PER_TRADE_SUS` strategies still in production rankings.
  Combined picture: **30 strategies** are likely to shift ranking once the
  remaining per-trade suspect set is recomputed — deferring this in the
  leaderboard is a known risk.
- **Data freshness is a recurring blocker.** SMA-34769 confirms a 7-day
  data gap (2026-07-11 → 2026-07-17). SMA-34777's three degenerate-timestamp
  equity CSVs (`vpvr_funding_regime_15m_20260711`,
  `vpvr_onchain_proxy_1h_20260711`, `vpvr_xs_leadlag_5m_20260711`) compound
  this: backtest runners are not emitting timestamped equity. SMA-34656
  (VPVR 4h detector) is still in_review, so the funding-carry strategy work
  (work-pool §funding-carry-asym) has effective data + detection pipeline
  inputs that are not yet complete.
- **Display path divergence between :8090 and :3210.** SMA-34780 surfaced
  `/cost-overlay` as a frontend-proxied but spec-absent route. Risk: silent
  5xx / missing widget in the UI. Worth a follow-up to either add the
  backend route or remove the frontend proxy.
- **Daemon configuration noise dominates log volume.** 4,022 occurrences
  (vs. next-most-frequent at 18) of `custom_env: blocked key skipped` for
  `MULTICA_DAEMON_MAX_CONCURRENT_TASKS` and `MULTICA_GC_TTL` indicate the
  allow-list hasn't caught up with the keys agents are setting.
- **Work-pool discipline is intact.** No `[DONE]` claim was fabricated in
  `~/.multica/work-pool.md` for issues whose status is still `in_progress`
  (34779, 34780, 34781). All other `[DONE]` claims are paired with verifiable
  completion comments.

---

## 4. Key metrics / artifacts index

| Metric / artifact | Value | Path / Source |
|---|---|---|
| `~/multica/` total | 7.0 GB | SMA-34767 |
| `/dev/nvme1n1p2` volume use | 47% (PASS, < 80% threshold) | SMA-34772 (`df -h`) |
| `node_modules/` | 2.4 GB | SMA-34767 |
| `apps/web/.next/cache/webpack` | 1.5 GB (reclaimable) | SMA-34767 |
| `quant-loop/strategies/` | 1.7 GB | SMA-34767 |
| Active strategies ranked | 44 (from 70 with equity CSVs) | SMA-34773 |
| `metrics.json` audited | 42 total | SMA-34777 |
| `metrics.json` daily-resampled | 25 (verified) | SMA-34777 |
| `metrics.json` newly Sharpe-computed | 12 | SMA-34777 |
| `metrics.json` nulled (degenerate timestamps) | 3 | SMA-34777 |
| BTC/ETH/SOL 1m/15m/4h coverage | 76.67% (stops 2026-07-10) | SMA-34769 |
| Autopilot backup files | 6/6 valid | SMA-34774 |
| Autorecens / agents / autopilots | 10 / 30 active | SMA-34770 |
| `daemon.log` lines | 164,724 in window | SMA-34768 |
| Most-frequent error pattern | `custom_env: blocked key skipped` × 4,022 | SMA-34768 |
| Iceberg detector artifacts | `iceberg_detector.py`, `run_detector.py`, tests, 2 flag CSVs | `~/multica/quant-loop/strategies/iceberg-detector/` |
| LOID-VPVR spec | `loid_vpvr_spec.md` (282 lines) | `~/multica_workspaces/…/5cfe3bd6/workdir/loid_vpvr_spec.md` |
| Updated AGENTS.md | post-redistribution topology | `~/multica/AGENTS.md` |
| Autopilot-backups audit report | `autopilot-backups-audit-2026-07-17.md` | `~/multica/ops-reports/` |

---

## 5. What remains open

1. **SMA-34779** — iceberg detector: artifacts exist on disk but no completion
   comment posted; need the agent to post a final summary including the
   actual detection method, scan window, and false-positive estimate.
2. **SMA-34780** — display health-check: status still `in_progress`; need the
   script path and pass/soft/fail counts.
3. **SMA-34781** — strategy-data non-empty audit: status still `in_progress`;
   need a final scan report (file count, corrupt/empty list) posted on the
   issue.
4. **Data backfill (2026-07-11 → 2026-07-17)** — not started; SMA-34769
   flagged all 9 BTC/ETH/SOL × 1m/15m/4h files as PARTIAL.
5. **18 `PER_TRADE_SUS` strategies** (SMA-34773) still carry a per-trade
   Sharpe in production metrics; ranking stability requires a full
   recompute rollout.
6. **Three degenerate-timestamp equity files** (SMA-34777) — update backtest
   runners to emit a timestamped equity CSV, then re-audit.
7. **`MULTICA_DAEMON_MAX_CONCURRENT_TASKS` and `MULTICA_GC_TTL`** — daemon
   custom_env allow-list drift (4,022 occurrences / 24h).
8. **Frontend `/cost-overlay` proxy** — exists in `next.config.js` but not
   in backend OpenAPI; either add backend or remove proxy.
9. **Continuous disk-trend monitor** (SMA-34772 follow-up) — convert the
   one-shot check into an hourly autopilot.
10. **Earlier-dated quantile `vpvr_obi` micro_reversion iter / funding-carry
    work** — items in work-pool still `[ ]` or `[~]`; rest of funding-carry
    prototype (line 45) untested.

---

## 6. Truth-or-uncertainty audit

- **Verified-fact claims** (the bulk of §2): each cites the SMA + comment id or
  artifact path. Re-running the originating command (e.g. `du -sh ~/multica/
  node_modules`, `df -h`, `git log --all -n200 --format=...`) reproduces.
- **Inferences.** "Most-frequent pattern is configuration-level" in §3 — supported
  by the counts but not by a code-trace linking each key to a single source of
  the warning.
- **Unverifiable.** Items 1–3 in §5 (open status of 34779 / 34780 / 34781):
  no posted completion comment at compile time. The on-disk artifacts for
  34779 are present and listed; what they actually *contain* (e.g. trade-size
  cutoff, sigma threshold, hit count) is not verifiable from the issue
  thread — only from running or inspecting the script.
- **Distinct from guess:** I did not infer these to be "good" or "bad" runs;
  I report what evidence is on hand.

— end of digest —
