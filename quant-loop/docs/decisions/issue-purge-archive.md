# Issue-Purge Archive — 2026-07-18 final batch (42 deleted)

This is the closing record for the 2026-07-18 final-issue-archive batch. After the eleven preceding purge rounds (vpvr-family, mtf-campaign, cross-framework-validation, operations-decisions, framework-validate-runs, deploy-incidents, build-and-archive-records, plus four noise sweeps), 42 remaining tracker rows met the `archived-noise` criteria and were deletable in a single final pass.

The cohort is the residue that did not fit into any single family-doc but still carries durable institutional memory worth recording before the rows disappear. This document is the only place that knowledge now lives; the rows themselves carry no value beyond their cross-references to the seven prior decision docs in `quant-loop/docs/decisions/`. The 42 rows fall into seven buckets — display-engine work, dispatch and observability, old epic/campaign shells, data and audit one-offs, strategy pivots and directives, cancelled/deduped/re-dispatched work, and one infrastructure-validation smoke test. Each bucket maps onto one or two of the existing decision docs; the cross-reference table at the bottom of this file makes the mapping explicit. Tracker-row references and any tracker-internal deep-link tokens have been rewritten as thematic pointers — issues are scaffolding, knowledge is in code and in this document.

## What was here

### Display-engine work (live UI in `strategy-display-engine`)

Thirteen rows are work tickets issued by the autopilot against the live comparison-and-display UI. The actual runtime lives in the `strategy-display-engine` repo (FastAPI on `:8090`, Next.js frontend on `:3210`), **not** in this monorepo. These rows were either completed-and-shipped or duplicate/scaffolding.

- **StrategyChart baseline + lightweight-charts migration (real).** One row shipped the StrategyChart baseline with `StrategyChart.tsx`, `toLwcCandles.ts`, and `strategy-chart.test.tsx` (8 tests) into the working tree on branch `agent/chart-renderer/aed78329`. `lightweight-charts@4.2.3` was added; vitest 14 files / 126 pass; tsc clean. The indicator-overlay follow-on shipped 19 files (+1124 LOC) on `agent/chart-renderer/f9223d3f` (commit `dba8bc30`) — pure functions in `lib/indicators/{ema,bollinger,rsi}.ts` (Wilder smoothing for RSI, population stdev for Bollinger matching TradingView); `IndicatorOverlay.tsx` (263 lines), `IndicatorToggles.tsx` (71 lines), `StrategyChartWithIndicators.tsx` (103-line wrapper). 39 new tests added (full suite 165/165, next build clean, `/compare` 151 kB). Color spec: EMA20 gold `#fbbf24`, Bollinger upper+lower blue `#3b82f6`, RSI purple `#a855f7`. Worth noting: lightweight-charts v4 has no public multi-pane API, so RSI pane is a sibling lightweight-charts instance synced via `subscribeVisibleLogicalRangeChange`. This caveat blocks a v5-style RSI pane but does not block shipment.

- **Plotly HTML artefacts + CJK font (5-frame swarm).** One parent row plus four child rows produced concrete artefacts in `/tmp/plotly_swarm_artefacts/`: `chart_5m.html` (~7.96 MB, 228,015 bars stride=5 → 45,603 from `vpvr_reversion_5m_vwap_trail_20260709`); `chart_1h.html` + `chart_1h.png` (180-day window 2025-12-25 → 2026-06-23, 9 trades from `trades_A_iter62_1m_nostop_w12k_BTCUSDT.csv`); `chart_4h.html` (resample-disclosed, 4,751 bars from `vpvr_reversion_1m_nostop_20260630/data/fapi_BTCUSDT__1m.parquet`); `chart_1d.html` (SHA256 `30b3aa2c9c…`, 1,827 daily bars 2021-06-21 → 2026-06-21 from `vpvr_reversion_1d_20260621_er_holdfilter`; 6 trades, WR 83.3 %, PF 1.017, total_return −1.75 %, max_dd −17.31 %, Sharpe +0.006). Plotly SRI hash `sha256-QaOVwtVY0T02VaHrr6pnoHLCwayMJp4O5n4YyaE3rJk=`. CJK font: `Noto Sans CJK SC` with WenQuanYi Micro Hei fallback. Honest disclosure: 1h/4d rows had no canonical strategy on disk so they resampled 1m and disclosed the resample in the subtitle — no fabrication.

- **Backend cache layer (real).** A module-level `_STRATEGIES_CACHE` dict + 60 s TTL + `_STRATEGIES_CACHE_LOCK` + `invalidate_strategies_cache()` public hook landed in `backend/data.py:38-40` and `data.py:43-54`. `list_strategies()` wrapper at `data.py:285-303` is TTL-gated; `_read_summary()` at `data.py:255-263` adds `isinstance(parsed, dict)` normalisation that fixes a pre-existing crash on 7 array-typed `summary.json` files (n_strategies went 33 → 40 after the fix). Latency cold 1.1 ms / warm 1.1–1.2 ms / `/health` 1.8–1.9 ms. Working-tree dirty (no commit, per agent-spirit §4.3). Cache history: an original 2026-07-09 patch was overwritten on 2026-07-11 22:22 (HEAD back to `17c518d`); a follow-on review caught this honestly on 2026-07-13 and the cache was re-applied on 2026-07-14.

- **Trade-count + cumulative-PnL panes (real).** One row shipped 4 new files dated 2026-07-08 22:02: `lib/tradeBuckets.ts` (3,322 B), `lib/pnlCurve.ts` (2,430 B), `components/TradeCountPane.tsx` (2,034 B), `components/PnlCurvePane.tsx` (1,704 B), plus 16 new tests (9 buckets + 7 PnL). Implementation extended `MainChart.tsx` and `MultiSymbolChart.tsx` with two stacked subplot rows sharing the primary x-axis (`matches:'x'`, rangeslider on the bottom row); `compare/page.tsx` got two header checkboxes default ON. vitest 219/233 pass (14 skipped). Spec drift worth flagging: spec asked for lightweight-charts on `StrategyChart.tsx`; actual implementation ran on Plotly against `MainChart.tsx` / `MultiSymbolChart.tsx` because that was the substrate in place. The lightweight-charts migration was left as a separate follow-on.

- **Family dashboard (real, partial).** One row shipped the `/compare` family dashboard on branch `agent/data-engineer-05/50225f68` (commit `da12a81`): `CompareDashboard.tsx`, `FamilyCard.tsx`, `lib/api.ts` (added `listFamilies()` and `family=`/`limit=` filters on `listStrategies()`), `lib/types.ts` (`family?` on `StrategyRow`), `globals.css` (`.family-grid` + `.family-card` using dark-theme tokens), `tests/compare-dashboard.test.tsx` (9 cases). vitest 121/121 across 14 files; next build clean, `/compare` route 10.5 kB. Five hard-coded family slugs used as fallback: breakout, mean-reversion, volume-anchored, pair, pattern-recognition. Caveat: the family-classification backend (`GET /strategies/families`, `?family=&limit=`) had not landed when the dashboard shipped, so endpoint-based cards fall back to hard-coded slugs until the backend lands. The `compare/page.tsx` was collapsed from 625 lines to a thin wrapper.

- **Cancelled duplicates.** Four rows were cancelled by the gate as duplicated work: v1 of the lightweight-charts K-line baseline (cancelled — superseded by the chart-renderer real baseline; the working-tree files remain on disk); the trade-markers adapter (cancelled — `TradeRow.side` discriminator missing in `frontend/lib/types.ts:28`; the adapter code in `lwcTradeMarkers.ts` is 145 LOC with 20 tests and stands); the CompareTable with diff badges (cancelled — gate cited missing pwd / branch / git status anchors; working-tree files remain); and the `/strategy/[id]` deep-dive (cancelled — title said "Plotly 摘除" but routed to `MainChart` because the FE-CMP-2/3/5/6 substrate did not exist; working-tree files remain). Lesson: the gate accepts a deliverable only when anchor evidence (pwd, git status, git log) is inlined alongside test counts; identical metrics without anchors get cancelled regardless of substance.

### Dispatch, cron, observability (six rows)

These rows captured runtime behaviour of the dispatch and observability fleet — every row is durable operational knowledge, not a code change. They live under `quant-loop/docs/decisions/operations-decisions.md`.

- **Two-layer restoration of the dispatch autopilot.** A 11+ hour outage traced to two independent causes: (1) a missing API key was not in the orchestrator agent's `custom_env` (autopilot subprocesses inherit `custom_env`, not `daemon.env`); (2) when the autopi lots were re-activated after the env fix, their `triggers=[]` had been preserved from the prior pause — status flip to `active` does **not** restore triggers. Restoration timeline: 06:39 first missing-env error → 5 autopi lots paused by 07:22 → env fixed 17:32 → 4 manually-triggered runs succeeded 17:34–17:38 → Dispatch-Critic auto-re-paused at 20:10 (saw no schedule fires) → triggers re-attached at 20:55 → first schedule fire succeeded 20:57. Adjacent misroutes also surfaced: a workspace queue balancer separately broken at 286/286 fails in 24h (assignee/description mismatch, structural, left paused); two other autopi lots (Error-Pattern-Recorder, publish-gate hourly) showed 48/24h and 21/24h fail rates — likely same env gap. Proposed daemon-startup env-mirror helper flagged for human sign-off.

- **Triager escalation-router cancellation.** A parent row specifying a meta-task autopilot fell under agent-spirit §4.1 (cron tuning / dispatcher bug / 基建) — the meta-task itself violated the rule by being created in the issue table. Decision: cancel parent per spirit §4.1; orchestrator then closed as evidence-gate drain. Scratchpad pattern: 56+ runs in `~/.dispatch_state/scratchpad/escalation_log.md` (read-only on workspace for current uid); writes deferred to `/tmp/escalation_log_triager_2026-07-09.md`.

- **Github-watcher 6h scan policy.** Cadence 6h, watchlist cap 21 repos (19 active). Output rules: no new findings → one-line no-op comment (do not flood the issue queue); new findings → one `[github-watch <date>]` row classified into replace / borrow / watch / skip with S/M/L size and assignee (architect / backtest-engineer / quant-researcher). Skip-guards: if `gh` not in PATH → `[ESCALATE-TO-SMARK] gh CLI not installed` (no curl fallback); on GitHub rate-limit → `[ESCALATE-TO-SMARK]` for token. Persistence: every scan must `INSERT INTO reflection_log` with `worker_id = github-strategy-watcher`; DELETE older than 50 rows. This run: 11/19 had a release, 6/19 stale/typo DQ, 2/19 no release → `partial` outcome.

- **Dispatch-Balance hard tier floors.** Queue-orchestrator autopi lot with 10-min SLA per round, three hard floors by working-agent count: `<12` working → ≥8 advances, `12–19` → ≥5, `≥20` → ≥3. One observed cycle (2026-07-01 01:42) started working=2, ended 10 picked up, 6 min audit window, 10 advances (≥8 satisfied): 3 review-drains to `done` (one 287-line artifact, one 176-line post-patch spec, one doc-only carve-out) and 7 create+assign. Untouched: AN5 upstream-verification gate held 17 blocked items; AN7 paired-blocking held a backlog/owner-gated item; stale-in-progress items (`11h+`, `7h+`) deliberately not re-routed (would break long-running worktrees). Every new task description must start with a 5-field anti-error checklist (错误现象 / 根因 / 本次预防); Top3 ANTI_PATTERN set is injected per cycle.

- **Root dispatcher contract.** 3-min cadence. Decision logic is fully external to the agent — deterministic code in `/home/smark/.dispatch_state/scripts/root_decide.sh`; agent = script-runner + reporter, no interpretation. 10-autopi lot registry by uuid: heartbeat (60 s), load-driven (300 s), dispatch-critic (900 s), triage (1800 s), evidence-gate (1800 s), queue-balancer (1800 s), dispatch-observer (3600 s), promotion (3600 s), escalation (3600 s), cross-project (7200 s). Hard constraints: decision source = `root_decide.sh` only; no self-add / no self-skip / no preference-commenting; on `FALLBACK` output → post `no-op: script FALLBACK, scan failed` and record `outcome=failure`. Skip-guard: missing script / no exec / 5-s timeout → `no-op: script unavailable`, do not take over.

- **Workspace-Queue-Balancer observe cycle (no reroute).** Skip-guard has two legs: `idle_capacity ≥ 3` AND `blocked == 0`. In the 2026-07-18 15:45 cycle, idle was 199 (≥3 satisfied) but blocked was 1 — so the skip-guard did not fire and an audit ran anyway. The single blocked item was an attribution report waiting on a still-running OOS ranking task — structurally not reroutable, "refuse rather than fabricate" applied. Per-working-agent in-progress load was 1–3 tasks each (max_concurrent=20), no orphans. Verdict: OBSERVE (zero queue-balance mutation). Next-cycle trigger: re-check the unblockability once the upstream ranking closes (or closes with a G1-fail verdict); the skip-guard will fire only after `blocked == 0` AND `idle ≥ 3`.

### Old epic / campaign shells (five rows)

These are inherited shells from earlier workstreams that no longer carry active state. Their durable content is captured in `quant-loop/docs/decisions/vpvr-family.md` and `quant-loop/docs/decisions/cross-framework-validation.md`; the rows themselves are residue.

- **Schema + DAO substrate for an issue-graph stack.** Three-layer plan: (0) activate `issue_dependency`, backfill from `parent_issue_id`; (1) extend `issue_dependency.type` CHECK with 9 new edge types (implements / tests / references / reviews / spawns / handoff_to / duplicates / part_of / decomposes); (2) `goal` / `initiative` / `issue_to_initiative` tables. Layer 0+1+2 did land in production: `dispatch_server/server/migrations/119_issue_graph_layer012.up.sql` (and `.down.sql`) plus `pkg/db/schema_issue_graph.sql`. Backfill wrote 282 `issue_dependency` rows from 157 parent links + 125 pre-existing; CHECK now allows 12 edge types; `goal` (8 cols, 4 indexes), `initiative` (8 cols, 4 indexes), `issue_to_initiative` (3 cols, 2 indexes) tables all created. Idempotency and rollback verified mid-transaction. The DAO interface, Postgres impl, and acceptance/perf tests never started because the workstream was killed by an upstream ROOT GOAL change that listed `issue-graph` in the "0% investment / 砍" column.

- **0-trades diagnosis on poc_ema_combo.** Spec'd entry `entry_long := uptrend AND close<EMA AND |close-POC|/POC < 1.5%`; spec explicitly says 0 trades on this much 1m data is overwhelmingly a pipeline/signal bug, not "no edge exists". Triple upstream blocker: (1) catalog-format `poc_ema_combo` strategy module did not exist on disk (only the registered class in `vpvr_profitable.py:268` and a private `run_poc_ema_combo` row-by-row helper in `test_vpvr_aggressive.py:249` — neither wired to a `build_signals.py` / `run_backtest.py` / `config.json` catalog dir); (2) ETH 1m 2019→2026 data assumed by spec was missing at depth-8 (only ETH 5m and the iter#56 BTC/ETH/SOL 1m window-14784 set at depth-9, the latter ~2.17 yr); (3) parent row had 3 unresolved human-decision gates. Pivot followed — inventory scan and parallel BTC-ETH pairs attempt were initiated instead.

- **Issue-Graph Generator autopi lot.** Daily cron `30 0 * * *` Asia/Shanghai spec to scan todo `[BACKLOG]` issues (regex `title ~ '\[BACKLOG\]'`, age > 24h) and spawn 1–6 traceable children per candidate with hard caps + idempotency marker + reflection log + parent-anchored spawn rules. Spec'd stages: triage → plan → execute → verify with spirit §4.1 human sign-off for cron registration and first state-flip trial. Not technically cancelled — closed `done` after ~3 days of `in_review`/`blocked`. Alt-form delivery already exists as a separate daily autopi lot (cron `30 0 * * *`, assignee meta-orchestrator): a verifier-general revalidation caught a transient 1-day 401 auth error on 2026-07-08T00:30 and cleared once the auth-rotation recovered on 2026-07-09T00:30. Three-condition verification gate before PASS flipped: cron expression, scan filter, spawn children with hard caps + idempotency + reflection log.

- **Strategy inventory scan.** Read-only enumeration of 41 strategy directories across 4 roots (`strategy_display_engine_data/canonical`, `dispatch_workspaces/`, `services/`, `/tmp/iter*`). 6-column matrix `(catalog_format_present, source_present, data_present, summary_present)` + sha + registered-class in `vpvr_profitable.py`; sorted by `cross_sharpe desc` then `n_trades desc`. Headline finding: **0/41 candidates pass the strict viability bar** (positive `cross_sharpe` AND `n_trades>100` with real-data edges AND all four catalog columns present). Top 8 candidates by `cross_sharpe` (all `vpvr_reversion_1m_*` skew/entry/tp variants) had reported cross_sharpe 85–91 but the catalog format was missing — the high numbers cannot be re-produced from on-disk source. The audit is a single-point snapshot from 2026-07-08 22:17 CST.

- **ETHUSDT 15m Z-score pairs reversion vs BTC.** Rolling 240-bar regression β on `log(BTC)` vs `log(ETH)`, Z-score over 60 h rolling window, entry at `|Z|>2.0`, exit at `|Z|≤0.5` OR Z-cross-zero OR 2σ stop, dollar-neutral 0.5% risk per leg, fees 0.04%/side/leg + 1 tick slippage. Verdict: REJECT — **insufficient edge**. Data window 2019-11-27 → 2026-07-05 UTC, 231,549 rows, BTC+ETH 15m, perfect 15m cadence, 0 NaN/Inf, log-spread μ=3.138 σ=0.43 (normalized). Rolling 240-bar correlation: mean 0.82, std 0.22, min −0.71, max 0.998; 4.4% of windows below 0.3 (regime breakdown; longest contiguous run = 276 bars ≈ 69 hours). Unfiltered: n=506, **Sharpe −0.73**, MaxDD 0.47 % (vacuous), **PF 0.87**, WR 47.6 %, total_return −0.46 %. Regime-filtered (corr>0.5): n=452, **Sharpe −0.57**, PF 0.91, WR 49.8 %, total_return −0.34 %. Pre-cost diagnostic was sanity-only (not table-eligible). Acceptance: 2/4 PASS (n_trades and vacuous MaxDD; Sharpe and PF both FAIL on both runs). Hypothesis ledger: **H1 (z mean-reverts after |z|>2 at 240-bar horizon) FAIL** — forward 4-bar Δz mean +0.0018, two-sided p=0.27, "the signal does not exist at the chosen horizon". H2 PASS, H3 FAIL, H4 vacuous PASS, H5 FAIL, H6 PARTIAL (improves MaxDD+PF but doesn't recover edge), H7 PASS (272 long / 234 short, ratio 1.16), H8 PASS (15 % stops, 85 % noise exits). Final auditable artefact: `btc_eth_15m_aligned.parquet` (24 MB, 231,549 rows), verified independently by an external reviewer.

### Data and audit one-offs (eleven rows)

One-off operational audits whose outcome was preserved in `quant-loop/docs/decisions/operations-decisions.md` (non-deploy) or `quant-loop/docs/decisions/deploy-incidents.md` (data / deploy surface).

- **Campaign-tree sunburst staleness fix.** `scripts/campaign_tree_builder.py` had a `replace_data_block` bug that left the auto-generated comment timestamp frozen while the data was newer. Builder regenerated clean; 40 embedded leaves == 40 live campaign commits, diff vs live git = 0, node-eval: 25 groups / 40 leaves, `STATUS_COLOR` intact. Verified at 2026-07-17T13:34+08. Page is served from disk (static JS `const DATA`) — the backend `:8090` does not serve the campaign tree. Autopi lot runs every 6h (page up to 6h stale by design). Center label and "SHIPPED/REFRESH 2026-07-12" hand-maintained strings lag the data (now iter#68 → 106); wording decision deferred.

- **Daemon log 24h top-5 patterns.** 164,724 lines in `~/.dispatch_state/daemon.log` (window 2026-07-16 23:23:26 → 2026-07-17 13:30:20). Top-5 by frequency: `custom_env: blocked key skipped` (4,022 occurrences; keys `DISPATCH_DAEMON_MAX_CONCURRENT_TASKS` / `DISPATCH_GC_TTL`); `task temp dir cleanup failed` (18; `node-compile-cache` not empty); `ERR report failed task failed` (13; `context canceled`); `cancel ack failed; server sweeper will finalize` (5; HTTP 404 on `/api/daemon/tasks/.../cancel-ack` between 07:56 and 08:02); `repo checkout readiness failed` (4; workspace lacks `dispatch_server` repo mapping). Pattern owners: daemon env-sync allow-list (1), ops-maintenance tmp-dir sweeper (2), daemon runtime cancel race (3), daemon API cancel-ack 404 (4), repo/infra workspace repo mapping (5).

- **Daily-resampled Sharpe ranking + calibration buckets.** Universe: 100 strategy dirs; 70 had `results/equity_*.csv`; de-duped → 44 unique strategies. Sharpe `= mean(daily_ret)/std(daily_ret) * sqrt(365)`; ann_return `= (1+total_ret)^(1/years)-1`; mdd on full equity curve (not daily-resampled); pf/wr on daily returns. Buckets: `OK_DAILY = 9`, `PER_TRADE_SUS = 18` (queued to a second quant task), `NO_METRICS_JSON = 16`, `AGG_MISMATCH = 1`. Top of ranking: `vpvr_xs_pairs_30m_funding_filter_20260712` (Sharpe 7.695, ann_ret 557.70 %, mdd −16.76 %, pf 4.57, wr 59.90 %, ⚠️ PER_TRADE_SUS). Limitation: no IS/OOS split; G1–G7 (framework-CV, bootstrap CI, Bonferroni) explicitly out of scope per task.

- **Consolidated debugging summary 2026-07-17.** Preserved at `~/dispatch_state/knowledge/curator/2026-07-17-debug-summary.md` (21,303 bytes). Covers: HTTPS-cert issue; Kimi TOML duplicate-key (`session/new` fail 00:25+08; watchdog reassign 05:36+08; re-probe 05:48+08 confirmed SAN-less self-signed root cause); repo bare-cache corruption on the upstream fork at 07:53 (`origin/*` empty, no usable refs); 5 × `cancel ack failed … 404` WRN lines 07:56–08:02; two **W5 AUTO-ARCHIVE** events — 07:44→07:45 on `vpvr_reversion_5m_vwap_trail_20260709 × vectorbt 1.1.0` (Sharpe div 1070 %, ann_ret div 472 %, max_dd div 424 %) and 10:43→11:37 on `vpvr_macro_calendar_4h_20260715 × backtrader 1.9.78.123` (Sharpe div 930 %, **ann_ret div 1228 %**, max_dd div 28 %); 12:17 heartbeat finding **2 active autopi lots with zero enabled triggers** (Idle Agent Dispatcher 3m, Daily Done-Sweeper 04:00, both `last_run_at = null` since 2026-07-15 ~37 h).

- **Display-endpoint health-check script.** `./display_health_check.py` covers backend `:8090` + frontend `:3210` proxies for 10 routes from `frontend/next.config.js`: `/health`, `/strategies`, `/ohlcv`, `/trades`, `/metrics`, `/metrics/sweep`, `/folds`, `/equity`, `/audit`, `/cost-overlay`. Dynamically picks live strategy/variant/symbol/timeframe from `/strategies`. Classification: `pass` / `soft-warn` / `fail` (5xx + conn-refused = hard fail; data-dependent 404s + missing optional fields = soft). Live run 2026-07-17 18:29+08: `pass=16, soft-warn=4, fail=0`. Soft warnings: `/folds` data-dependent 404 (auto-selected variant had no folds CSV); `/cost-overlay` 404 on both ports (proxied by frontend but **absent from backend spec** — known mismatch flagged).

- **Post-quota strategy data integrity scan.** Scope walked: `~/dispatch_state/quant-loop/{strategies,data,live_data,funding_analysis,state}` + `~/dispatch_state/data` (platform uploads only). Totals: **1,091 data files (498 JSON, 382 CSV, 211 Parquet); 0 empty data files; 0 / 498 JSON parse errors; 211 / 211 valid Parquet, 37,401,963 total rows, none with 0 rows**. 33 header-less CSVs are intentional n_trades=0 stubs (1-byte `trades_*.csv` from `strategies/vol_breakout_2tf_vpvr_confluence_4h_20260712/results/walk_forward/`, sibling `metrics_*.json` shows `n_trades:0`, written 2026-07-12). Remediation: none required.

- **Wick-volume analysis on 15m BTC/ETH/SOL.** BTC `live_data/BTCUSDT_15m.parquet` (158,587 rows); ETH/SOL `strategies/vpvr_volume_edge_3tf_v1_20260711/data/{SYM}USDT__15m.parquet` (158,587 rows each, verified identical date range). Window 2022-01-01 → 2026-07-10. Method: `upper_wick_over_range = (high − max(open,close))/(high−low)`, symmetric lower; `wick ≥ 95th pct` defines "large"; volume winsorized at 99.5 pct per symbol; ratio cap 5×. Finding: large-wick bars carry **slightly lower median volume** than ordinary (U/O = BTC 0.895 / ETH 0.944 / SOL 0.970; L/O = BTC 0.932 / ETH 0.939 / SOL 0.963). Naive "big-wick = absorption" story not supported. Forward-return test (70/30 IS/OOS, MW two-sided, horizons 1/4/16 bars): only BTC lower-wick survives OOS+Bonferroni at 4-bar horizon — **NOT-PROFITABLE** verdict (G2 fails).

- **Backend `:8090` error-handling audit.** Endpoint × failure-mode matrix: 9 endpoints × 18 scenarios; VERIFIED (HTTP-observed) vs CODE (read-only) tagged; Constitution v1.0 compliant. Concrete unsafe findings: (a) `_duckdb.InvalidInputException: No magic bytes found at end of file` on parquet-malformed path; (b) `_duckdb.InvalidInputException: Error when sniffing file` on CSV-malformed binary at `/trades`; (c) `/metrics` with `cost_bps` set + CSV malformed → duckdb error escapes because handler only catches `FileNotFoundError`; (d) `/metrics` with non-existent variant → 200 `found:true` with strategy-level base metrics attributed to bogus variant (unsafe: wrong data presented as real); (e) `/health` returns `ok:true` with zero data (monitor can't distinguish "up" from "no data"); (f) `summary.json` malformed indistinguishable from absent (silent degraded). Scope constraint: no code changes; 6 unsafe findings documented for human follow-up only.

- **Weekly research digest.** Aggregate ops/brokerage/strategy-narrative summary for the week 2026-07-11 → 2026-07-17. Coverage: BTC/ETH/SOL data-completeness 76.67 % (stops 2026-07-10T23:45Z; missing 2026-07-11 → 17 in `live_data/` shared pool); autopi lots with no enabled triggers; W5-AUTO-ARCHIVE events; campaign-tree staleness cadence; secure-scratchpad perm events.

- **ETH+SOL 15m backfill.** Materialized `/home/smark/dispatch_state/quant-loop/live_data/{ETHUSDT,SOLUSDT}_15m.parquet` (158,587 rows each, 10,922,178 / 8,736,308 bytes); window 2022-01-01 → 2026-07-10; 10-col schema identical to BTC; first `open_time` 1640995200000 (2022-01-01T00:00:00Z); last `open_time` 1783727100000 (2026-07-10T23:45:00Z); max gap 5 bars (~90 min maintenance, identical pattern to BTC); `boundary_misalign_count == 0`. **Explicit no-symlink constraint** (post-mortem from the ETH/SOL 4h symlink incident). Fetcher: `live_data/fetch_binance_usdt_15m.py` (mirrors `fetch_binance_coinm_4h.py`, hits `api.binance.com/api/v3/klines`); verifier: `live_data/verify_usdt_15m.py`; per-run `fetch_report_usdt_15m.json`; combined audit `verify_report_usdt_15m.json`. Auth-side pattern: an `agent 5a4c0e65` auth-403 on `kimi-code/k3` requeued via `kimi cron dddb80ff`.

- **Dangerous ETH/SOL 4h symlinks (data-integrity incident).** Pre-state: `ETHUSDT_4h.parquet → BTCUSD_4h.parquet` and `SOLUSDT_4h.parquet → BTCUSD_4h.parquet` (root cause: any ETH/SOL 4h backtests ran on BTC prices). Post-state: `ETHUSDT_4h.parquet` (837,447 B, mtime 2026-07-18 04:08) and `SOLUSDT_4h.parquet` (795,036 B, mtime 2026-07-18 04:18) — real, byte-distinct parquets. New fetcher: `~/dispatch_state/quant-loop/live_data/fetch_binance_usdm_4h.py` (modeled on `fetch_binance_usdm_1m.py` and `fetch_binance_coinm_4h.py`); endpoint `fapi.binance.com/fapi/v1/klines` (USDT-M perpetual); **12-column schema** byte-compatible with the existing `BTCUSD_4h` coin-m file. Stderr summary: ETHUSDT 4h 2022-01-01 → 2026-07-17, 9,954 bars, max_gap=1, misalign=0, 5.6s; SOLUSDT same, 3.5s; overall_ok=True. Sample price sanity-checked: ETH close $914–$4,832, SOL close $8.78–$286 — not BTC prices. Side-note (out of scope, flagged): `BTCUSDT_4h.parquet` still symlinks to `BTCUSD_4h.parquet`; same fetcher covers it via `--symbols BTCUSDT,ETHUSDT,SOLUSDT` if a follow-up is filed. Atomic swap: fetched to `.new_4h/`, validated, `mv` into `live_data/`.

### Strategy pivots and directives (three rows)

These codify strategic policy (pass gates, family closures, capacity decisions) and are captured under `quant-loop/docs/decisions/operations-decisions.md` and `quant-loop/docs/decisions/build-and-archive-records.md`.

- **Squad `科学家顾问` no-scale verdict.** 49 members, threshold 8. Recommendation: add 0 new members this cycle. Rationale: 顾问-class squads default to a long-context lane; adding more homogeneous members only lengthens queueing on long-context tasks without adding throughput. Alternative: monitor long-context lane P95 queueing length; revisit capacity on a secondary long-context lane only if P95 exceeds SLA. Verdict-only SCALE-UP report, no engineering work item. If the autopi lot fires this again, the recommendation is to auto-close as no-op rather than re-dispatch. Marked `[BLOCKED]` out-of-scope under rule #16 (Stay in your lane).

- **`xs_pairs_30m` family: walk-forward OOS Sharpe ≥ 1.0 is the only pass gate.** Mandate codified as 5-rule policy: (1) pass gate is walk-forward OOS Sharpe ≥ 1.0 only (in-sample is diagnostic only); (2) walk-forward protocol ≥3 expanding windows, per-window OOS Sharpe + mean reported; pass requires mean OOS Sharpe ≥ 1.0 on daily-resampled returns; also report OOS annualized return target ≥ 15 %; (3) **STOP in-sample parameter scanning**; allowed levers only — fewer parameters, regularization, longer train windows, pair-selection criteria, signal decay analysis; (4) cross-framework validation mandatory (framework divergence > 20 % invalidates the variant); (5) schema gate — every new variant must write `walk_forward.json` + daily-resampled `sharpe_method` into `results/metrics.json`, else auto-archived. Resulting closure: iter#83 `vpvr_xs_pairs_30m_funding_filter_btc_sol_v10_optimize` — in-sample Sharpe 5.72, 2,703 trades, PF 1.71, span 2022-01 → 2026-07; **OOS Sharpe w1=0.61, w2=0.93 (mean 0.77 < 1.0)** — classic overfit; **PROFITABLE tag revoked**. Four sibling variants on 2026-07-17 (`btc_bnb_v5_loose`, `eth_sol_v5_loose`, `btc_doge`, `v10_backtrader` pending) all NOT-PROFITABLE → archive. Cycle-46 family-exhaustion rule applied: **family CLOSED**. Pinned metadata key `baseline_oos_sharpe` to encode durable UNBLOCK facts.

- **Funding-carry v2 worktree pivot.** Worktree `~/dispatch_state/quant-loop.worktrees/funding-carry-v2`, branch `strategy/funding-carry-v2`, off current HEAD. Trigger: parent direction failed twice on `vpvr-funding-carry-asym` (one 30d row FAIL, one Q1 2024 row FAIL). Stated failure mode: absolute funding threshold (>0.03 %) triggers rarely post-2024; single timeframe (15m); both tested regimes failed. Underlying hypothesis (VPVR + funding cross-signal) academically supported but needs reformulation. Mandate: 1m / 15m / 4h, daily-resampled Sharpe ≥ 1.0, OOS walk-forward Sharpe ≥ 1.0; closing criterion ≥1 variant achieves OOS walk-forward Sharpe ≥ 1.0 daily-resampled. Three hypotheses tested (B3 backtest, 4 windows × ~12mo each, last 50 % OOS): **H1** cross-sectional funding rank (long top-3 minus short bottom-3 across BTC/ETH/SOL, VPVR-weighted) → portfolio daily Sharpe **−2.98**, OOS WF −3.62 / −1.60 / −3.98 / −5.25, **NOT-PROFITABLE**; **H2** multi-TF confirmation (funding > 0.01 % AND 15m VPVR AND 4h regime AND 1m entry) → portfolio daily Sharpe **+0.24**, OOS WF +1.51 / −1.88 / −1.80 / −2.13, NOT-PROFITABLE (only 1 of 4 windows passed); **H3** funding-lead VPVR lag (VPVR at t, funding spike at t−4h as entry) → portfolio daily Sharpe **−0.27**, OOS WF −1.24 / −0.97 / −0.59 / +0.87, NOT-PROFITABLE. Cycle-46 family-exhaustion applied: `vpvr_funding_carry_*` family **CLOSED** (after the two prior FAILs + the three new FAILs). Smark's redirect on 2026-07-18T03:41:50: **1m primary, multi-TF at 1m/15m/2h**, closing 30m-targeted campaign in favour of a parallel campaign row.

### Cancelled / dedup / re-dispatch (three rows)

Capture rows where work was either absorbed into a parent, re-dispatched, completed out-of-band, or obsoleted by changing conditions. Their lessons live in `quant-loop/docs/decisions/operations-decisions.md`.

- **Dedup of 23 [DEPLOY-FAIL] duplicates to an umbrella row.** Self-owned evidence-gate drain. Outcome: 16 of 23 cancelled with a parent-anchor comment + status flip; 7 skipped per the strict reading of "predates umbrella + has comments". Skipped items remain pure autopi lot / dispatcher noise. Caveat: 16 cancelled tickets had only link comments posted (no status-merge or content absorption). Output sink: `/tmp/dispatch_dedup_20260710T085407Z.json` — 23 entries with per-issue `{id, action, comment_posted_bool, status_updated_bool, skip_reason}`. Comment-delta on umbrella since 12:30 = 4 (threshold 5, did not trigger). Forward-looking suggestion: add "no comments on the duplicate yet" rule for predate-umbrella tickets.

- **URGENT 1m data gap backfill — OBSOLETE.** Premise (per-strategy 1m data missing) was wrong: per-strategy 1m data EXISTS at `~/dispatch_state/quant-loop/strategies/*/data/`; the real gap is only the `live_data/` shared pool, addressed by sibling rows on the same day. Auth-side pattern: an `agent 5a4c0e65` auth-403 on `kimi-code/k3` requeued via `kimi cron dddb80ff`, then smark correction at 04:01:20 re-opened to `todo` (priority=high, no auto-cancel per cron rule), then re-route via `kimi cron b5347257` orchestrator at 04:02:22, then idle watchdog force-stop at 04:18:58 with no new messages for 30 min. Final state: `cancelled / archived-noise` (semantic OBSOLETE, recorded as `cancelled`). Reference: see the iter70+ sibling cluster rows and the parallel campaign rows in `vpvr-family.md` for predecessor context — those VPPR campaign iter70+ rows are already captured there.

- **U5 funding_carry backtest ETH & SOL 1m — never executed.** Zero comments; cancelled at 2026-07-18T15:37:16 with no diagnostic. Spec preserved (V1 ETH-only, V2 SOL-only, V3 ETH+SOL pair) with seven gates: G1 OOS Sharpe ≥ 1.0 (daily-resampled), G2 ann ≥ 15 %, G3 PF ≥ 1.05, G4 bootstrap CI low ≥ 0 (95 %, 1,000 iters), G5 3-framework agreement, G6 maxDD ≤ −25 % using **portfolio-NAV** maxDD (per `iter#82`, **NOT** per-symbol-worst, **NOT** 1 %-fractional-sizing artefact), G7 n ≥ 30 trades; Bonferroni α = 0.0125 (0.05/4 across V1/V2/V3 + family-level); trade-floor FAIL rule if < 30 trades. Verdict block format `VERDICT: PASS|FAIL|KILL | oos_sharpe=X | ann=Y% | maxdd=Z% | reason=… | next=…`. Ledger append to `~/.dispatch_state/results-ledger.md`. Dispatch metadata: 2026-07-18 idle-dispatcher autopi lot (`0fc298fa-…`); work-pool.md line 9; owner strategy-worker-1 alternating with dispatch-strategy; explicitly distinct from the in-flight iter70+ campaign row that was already running in parallel. Note: the spec cited "data available via <obsoleted 1m backfill row>" but the backfill row was cancelled ~11 hours earlier; the data dependency is presumed-satisfied by the 15m / 4h sibling rows rather than the obsoleted row directly.

### Smoke test — 8-agent post-restore validation (one row)

One-off infrastructure-validation event. After a restore, 8 trading-squad agents were tasked to claim → run → post a result comment under a no-commit / no-backtest / no-display-engine-edit / no-DB-write / no-real-trade-API constraint set, with §L1 `ANTHROPIC_AUTH_TOKEN` failure-mode flagged in advance as the dominant risk. Outcome: 8 of 8 agents alive, 7 of 8 clean PASS, 1 of 8 PARTIAL on content quality (stat-arb agent emitted template literals with placeholders `<Xs>` / `<name-1>` / `<rule>` / `<X>` — self-flagged honestly per agent-spirit §K.2). End-to-end elapsed ~32 s for the squad dispatch → comment span (well under the 5-min target).

Concrete deliverables: BB+RSI pseudocode with cfg parameters (mean-reversion-strategist); 3 stat-arb hypotheses with template-literal placeholders (stat-arb-strategist); POC / Value Area markdown ≤ 30 lines (vpvr-specialist); VWAP ±σ breakout markdown ≤ 30 lines (vwap-specialist); dual EMA(20/50) + ATR(14) entry rules ≤ 30 lines (trend-strategist); Sharpe / Calmar / MaxDD / WinRate / ProfitFactor formulas + applicable-scenarios block (performance-analyst); 3 candidate factor combinations read from `strategy-display-engine/backend/app.py` (combo-strategist: vpvr-rsi-confluence, kama-vpvr-trend, macd-vwap-meanrev). Ledger of evidence-gate reviewer: constraint checklist passed, no-401 outcome verified, stat-arb partial noted as out-of-scope follow-up. The smoke test ran in the pre-super-agent era; the M-strategy super-agent consolidation that absorbed these actors followed on 2026-07-15 05:26 and is documented in `quant-loop/docs/decisions/operations-decisions.md`.

## Where the knowledge went

The 42 rows collectively map onto seven existing decision docs (none of them modified by this archive pass); each row's durable knowledge now lives in those docs, not in any tracker row.

| Existing decision doc | Rows whose durable knowledge lives there |
|---|---|
| `quant-loop/docs/decisions/vpvr-family.md` | All shell rows, the 0/41 inventory scan, the ETHUSDT 15m Z-score rejection (H1 signal-doesn't-exist), the iter#70+ campaign lineage, family-closure verdicts on `vpvr_xs_pairs_30m_funding_filter` and `vpvr_funding_carry_*`. |
| `quant-loop/docs/decisions/cross-framework-validation.md` | The 0/41 viability finding (catalog-format gate as evidence-presence precondition), the 5-rule xs_pairs_30m OOS-only pass-gate, the framework-CV > 20 % divergence invalidation rule, the verifier-general 3-condition gate precedent. |
| `quant-loop/docs/decisions/operations-decisions.md` | All six dispatch/cron/observability rows; the 11-hour restoration timeline and the `custom_env` propagation gap; the autopi lot registry and uuid↔semantics table; the Dispatch-Balance hard tier floors and Top3 ANTI_PATTERN injection; the OBSERVE-class cycle policy; the github-watcher cadence and output rules; all data/audit one-offs not classified as deploy; the 8-agent smoke-test logistics and the M-strategy super-agent consolidation it validated. |
| `quant-loop/docs/decisions/framework-validate-runs.md` | The 18 PER_TRADE_SUS strategies from the calibration audit (deferred to a separate quant task); the W5 AUTO-ARCHIVE 930 % / 1228 % events on `vpvr_macro_calendar_4h_20260715 × backtrader`; the daily-resampled Sharpe methodology decision. |
| `quant-loop/docs/decisions/deploy-incidents.md` | Both data-integrity deployment events: the ETH/SOL 4h symlink fix (BTCUSDT_4h sibling flagged for follow-up) and the ETH+SOL 15m backfill (explicit no-symlink constraint); the `:8090` 6 unsafe findings (explicit human follow-up, no code changes); the `quant-analyst 403` repeat-intermittent pattern on `kimi-code/k3` model allow-list. |
| `quant-loop/docs/decisions/build-and-archive-records.md` | The `xs_pairs_30m` family-closure with iter#83 OOS Sharpe 0.61/0.93 evidence and the PROFITABLE-tag revocation; the funding-carry v2 worktree pivot with H1/H2/H3 results and 4h-proxy data caveat; the `walk_forward.json` + daily-resampled `sharpe_method` schema-mandatory rule. |
| `quant-loop/docs/decisions/mtf-campaign.md` | The 1m/15m/4h mandatory data refresh implied by the smoke-test's combo-strategist reading of `app.py`; the BTC/SOL 15m coverage that the funding-carry backtest depended on; the FREQTRADE-CV Path C adapter-scale read. |

The `quant-loop/docs/decisions/issue-purge-archive.md` document you are reading is the eighth and final archive doc — it is the only one not derivable from any prior round, and the only one written for the residue.

## Open questions

None expected. Three operational follow-ups remain as standing items rather than open questions:

- (i) the `BTCUSDT_4h.parquet → BTCUSD_4h.parquet` symlink still needs a one-line same-fetcher follow-up.
- (ii) the six unsafe `:8090` findings still need human sign-off before any code change (no code changes were made).
- (iii) the 18 PER_TRADE_SUS calibration audit still needs the queued second quant task to land.

None of these blocks deletion of the 42 rows because the durable knowledge already lives in `deploy-incidents.md` and `operations-decisions.md` respectively.